# 方案 C：企業級多代理 SEO 平台 — 完整開發計畫

> **版本：** v2.2 (一致性清理 + Phase 排程修正 + RLS 強化) | **日期：** 2026-02-15
> **基礎架構參考：** OpenClaw v2026.2.13（MIT License）
> **目標：** 構建業界首個「AI 多代理協作」的企業級 SEO 工作平台

---

## 📌 本地參考資源（工程實作對照）

為了降低 Phase 0-1 的工程落差與返工成本，本專案在開發期間會**持續對照** OpenClaw 作為 reference implementation：

- **本地路徑**：`C:\Users\User\Desktop\openclaw`
- **定位**：工程實作參考（設計/模式/實作細節對照），**不是**本平台的 runtime 依賴或需要直接 fork 的程式碼來源。
- **主要對照範圍**：啟動序列與配置驗證、事件系統、Plugin discovery/註冊、Agent scope/workspace 隔離、Cron 排程服務、Docker sandbox。
- **使用規範**：以本文件的規格與驗收標準為主；當實作細節不確定時，再回到 OpenClaw 對照其成熟模式，避免自行發明導致不可維護。

---

## ⚠️ 架構設計評審修正報告 (v2.0)

經過 OpenClaw 源碼深度審查 + 架構設計評審後，本計畫已針對以下 6 項重大議題進行修正：

### 修正 1：代理分層 — Smart Agents vs Auto Tasks (重要)
- **問題**：原計畫將 12 個功能全部包裝為「AI 代理」，但 `pagespeed-agent`、`serp-tracker`、`schema-agent` 等不需要 LLM 推理，硬包成 Agent 增加系統複雜度與 API 成本。
- **修正**：將 12 個單元分為 **Smart Agents**（需 LLM 推理循環）和 **Auto Tasks**（確定性管線，`model: null`）。兩層共用同一套 Agent Runtime（workspace 隔離、事件發送、cron 排程），但 Auto Tasks 不啟動 LLM 推理循環，節省成本並降低複雜度。

### 修正 2：Orchestrator 統一為 BullMQ Flow (重要)
- **問題**：原計畫同時提及 Event Bus、BullMQ、Temporal 三者，分工不清。Temporal 需獨立 Server cluster + Elasticsearch，對 8 人團隊維運成本過高。
- **修正**：選定 **BullMQ Flow** 為唯一工作流引擎（parent-child job dependency + retry + delay + priority）。Event Bus 降級為純通知/日誌層，不承擔業務邏輯。不引入 Temporal，不實作 Saga Pattern（SEO 場景用 failed + 通知人工即可）。

### 修正 3：SERP 資料取得策略 — API-First (關鍵)
- **問題**：原計畫大量依賴 Google SERP 自建爬取，面臨法律風險（Google ToS 禁止自動化查詢）、技術風險（反爬軍備競賽）、成本風險（Residential Proxy $8-15/GB，原成本估算未反映）。
- **修正**：重構為三級資料源策略：
  - **L1（合法、免費/低成本）**：Google Search Console API + Google Analytics 4 API
  - **L2（付費 SaaS、風險轉嫁）**：Ahrefs/SEMrush SERP API、ValueSERP/Scale SERP ($50-100/月)
  - **L3（極低頻率補充）**：自建 Playwright 爬取，僅用於 L1+L2 無法覆蓋的場景

### 修正 4：儲存層簡化 (MVP 優先)
- **問題**：原計畫同時使用 PostgreSQL + TimescaleDB + pgvector + Redis + MinIO + SQLite（5-6 種），MVP 階段過度複雜。
- **修正**：MVP 僅使用 **PostgreSQL + pgvector + Redis**（3 種）。TimescaleDB 待資料量超過千萬行再啟用（僅需 `CREATE EXTENSION`，無遷移成本）。MinIO 改用 S3 或本地 FS。

### 修正 5：LLM 模型 — Step-Level Routing + 統一閘道
- **問題**：原計畫對每個代理固定指定一個 LLM，選擇邏輯不一致（如 `technical-auditor` 幾乎不用 LLM 卻配 GPT-4o）。同時維護 Anthropic + OpenAI 兩套 SDK 增加負擔。
- **修正**：改為 **step-level model routing**（同一代理不同步驟可用不同模型），並透過 **Vercel AI SDK** 統一抽象層管理多 provider，不自行維護兩套 SDK。

### 修正 6：爬蟲策略務實化
- **問題**：原自建 Scraping-Infra 過重（Stealth Plugin + Proxy Pool + Captcha Solver），法律與技術風險高。
- **修正**：爬蟲降為三級備援（見修正 3）。技術審計的全站爬取（`technical-auditor`）因為是爬取客戶自有網站，不涉及反爬問題，保留 Playwright 直接爬取。`competitor-monitor` 的競品數據亦改為 API-First（優先 Ahrefs/SEMrush 競品 API，自建爬取僅為補充）。

### 修正 7：多租戶資料模型 — DB 層強隔離 (v2.1 新增)
- **問題**：v2.0 安全架構提及「客戶數據隔離（多租戶）」，但資料表設計以 `projects` 為頂層，缺少 `tenant`/`org` 實體與 `tenant_id` 欄位。無法在 DB 層做 RLS (Row-Level Security) 強隔離，難通過企業資安審查。
- **修正**：新增 `tenants` / `users` / `memberships` 三張核心表。所有業務表（`projects`, `keywords`, `content`, `backlinks`, `page_audits`, `rank_history`）皆透過 `projects.tenant_id` 可推導租戶歸屬。PostgreSQL RLS Policy 確保跨租戶查詢在 DB 層即被攔截。

### 修正 8：Outbox Pattern — 事件投遞一致性 (v2.1 新增)
- **問題**：Event Bus 降級為通知/日誌層後，若 DB 寫入成功但通知未發（或反之），會導致狀態、事件、UI 即時流不同步——這是企業運維最常踩的坑。
- **修正**：在 DB transaction 中同步寫入 `events_outbox` 表，由獨立 dispatcher 可靠投遞到 WebSocket/Slack。確保「業務狀態變更」與「事件發送」的原子性。

### 修正 9：Vercel AI SDK Spike + 步驟級 LLM 路由 (v2.1 新增)
- **問題**：Vercel AI SDK 主要為前端/edge runtime 設計，在 Node.js 後端長時間 Agent 場景（多輪 tool use、streaming、retry、token counting）的支援程度未驗證。且 step-level model routing 尚未在各代理規格中具體定義。
- **修正**：Phase 0 必須進行「AI SDK Spike」驗證（見 Phase 0 任務）。各 Smart Agent 規格中新增步驟級路由表（哪些步驟用 smartModel、哪些用 cheapModel），避免開發者自行決定導致成本失控。

### 修正 10：文件一致性清理 + Phase 排程修正 (v2.2 新增)
- **問題**：v2.1 新增多項內容後，產生以下內部矛盾：(1) Temporal/Saga 的「不使用」說明散落多處，易被誤讀為仍在考慮；(2) `create_hypertable` 指令散落 4.4/7.3/8.1，與「集中到 8.3」原則矛盾；(3) Outbox 索引欄位名不一致（表用 `dispatched`，索引策略寫 `status`）；(4) RLS 僅示範 `projects`，其他業務表缺統一模板；(5) Phase 0 工作量膨脹但仍標 2 週；(6) Phase 4 仍列「多租戶架構」與 Phase 0 已完成的 DB 層多租戶重疊；(7) 架構圖未區分 Smart Agent / Auto Task。
- **修正**：全文統一清理——Temporal/Saga 僅在修正 2（決策紀錄）保留；`create_hypertable` 僅留在 8.3；Outbox 索引改為 `(dispatched, created_at) WHERE dispatched = false`；RLS 補統一模板 + Middleware 規格；Phase 0 擴為 3-4 週；Phase 4「多租戶」改為應用層功能（租戶管理 UI / 計費配額 / 租戶自助 onboarding）；架構圖加 🧠/⚙️ 標記。

---

## 目錄

1. [專案總覽與願景](#1-專案總覽與願景)
2. [系統架構設計](#2-系統架構設計)
3. [6 Smart Agents + 6 Auto Tasks 定義](#3-6-smart-agents--6-auto-tasks-定義)
4. [核心模組技術規格](#4-核心模組技術規格)
5. [數據流與代理協作機制](#5-數據流與代理協作機制)
6. [從 OpenClaw 借鑑的架構模式](#6-從-openclaw-借鑑的架構模式)
7. [技術選型與堆疊](#7-技術選型與堆疊)
8. [資料庫與儲存設計](#8-資料庫與儲存設計)
9. [API 設計與整合清單](#9-api-設計與整合清單)
10. [安全架構](#10-安全架構)
11. [Web Dashboard 設計](#11-web-dashboard-設計)
12. [開發階段與里程碑](#12-開發階段與里程碑)
13. [團隊組成與分工](#13-團隊組成與分工)
14. [成本估算](#14-成本估算)
15. [風險矩陣與應對策略](#15-風險矩陣與應對策略)
16. [商業化路線圖](#16-商業化路線圖)

---

## 1. 專案總覽與願景

### 1.1 問題陳述

現有 SEO 工作流程的痛點：
- SEO 專員每天在 5-10 個 SaaS 工具間切換（Ahrefs、SEMrush、GA4、GSC、Screaming Frog...）
- 80% 的時間花在重複性的數據搜集與報告整理
- 競爭對手分析仍需大量人工爬梳
- 團隊間缺乏統一的 SEO 知識庫與決策歷史
- 從「分析結果」到「實際優化行動」之間存在斷層

### 1.2 解決方案

構建一套「6 Smart Agents + 6 Auto Tasks」協同工作的企業級平台，各單元負責 SEO 流程中的專業環節，透過 **BullMQ Flow** 工作流引擎協調，實現從「關鍵字研究」到「內容發布」到「成效追蹤」的完整閉環自動化。Smart Agents 負責需要 LLM 推理的創意/分析工作，Auto Tasks 負責確定性的資料收集/爬取/計算工作，兩者共用同一套隔離的 Agent Runtime。

### 1.3 核心價值主張

```
┌──────────────────────────────────────────────────┐
│           傳統 SEO 工作流程（人工驅動）             │
│                                                    │
│  專員 → Ahrefs → Excel → 寫文章 → WordPress       │
│       → GA4    → PPT  → 改Meta → GSC 驗證         │
│       → Screaming Frog → 報告 → 會議 → 下一輪     │
│                                                    │
│  ⏱️ 每週 40+ 小時   💰 工具月費 $500+              │
└──────────────────────────────────────────────────┘
                        ↓ 轉換為
┌──────────────────────────────────────────────────┐
│           AI 多代理 SEO 平台（自動化驅動）          │
│                                                    │
│  人類 → 下達策略指令 → Orchestrator 分派           │
│       ← 收到報告/草稿 ← 審核 → 批准執行           │
│                                                    │
│  ⏱️ 每週 5-8 小時（僅審核決策） 💰 完全自控        │
└──────────────────────────────────────────────────┘
```

---

## 2. 系統架構設計

### 2.1 高層架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                        Web Dashboard (React/Next.js)            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ Keyword  │ │  Rank    │ │ Content  │ │ Reporting│          │
│  │ Explorer │ │ Tracker  │ │ Editor   │ │ Center   │          │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘          │
└───────┼─────────────┼───────────┼─────────────┼────────────────┘
        │             │           │             │
        ▼             ▼           ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     API Gateway (REST + WebSocket)               │
│         Token Auth │ Rate Limiting │ RBAC │ Audit Log           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                   🧠 Orchestrator (BullMQ Flow 工作流引擎)         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                  Message Bus (Event-Driven)               │   │
│  │          借鑑: OpenClaw agent-events.ts 事件系統           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ Task     │ │ Agent    │ │ Priority │ │ Dependency│          │
│  │ Queue    │ │ Router   │ │ Scheduler│ │ Resolver  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└──────────────────────────────────────────────────────────────────┘
        │
        ▼ BullMQ Flow 分派任務給 6 Smart Agents + 6 Auto Tasks
┌──────────────────────────────────────────────────────────────────┐
│                    Agent Pool（代理池）                            │
│                                                                   │
│  🧠 Smart Agents:                                              │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │🧠Keywrd│ │🧠Contnt│ │🧠Compet│ │🧠Backlk│ │🧠Report│       │
│  │Research│ │Writer  │ │Monitor │ │Builder │ │Genertr │       │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘       │
│  ┌────────┐                                                    │
│  │🧠Contnt│                                                    │
│  │Refresh │                                                    │
│  └────────┘                                                    │
│                                                                │
│  ⚙️ Auto Tasks:                                                │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │⚙️Tech  │ │⚙️Schema│ │⚙️Intrnl│ │⚙️PgSpd │ │⚙️Local │       │
│  │Auditor │ │Markup  │ │Linking │ │ Agent  │ │SEO     │       │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘       │
│  ┌────────┐                                                    │
│  │⚙️SERP  │                                                    │
│  │Tracker │                                                    │
│  └────────┘                                                    │
└──────────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Infrastructure Layer                            │
│                                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ Browser  │ │ Memory   │ │ Scheduler│ │ Docker   │           │
│  │ Engine   │ │ Store    │ │ (Cron)   │ │ Sandbox  │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ API      │ │ Notifi-  │ │ File     │ │ Proxy    │           │
│  │ Integra. │ │ cation   │ │ Storage  │ │ Pool     │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 核心設計原則

| 原則 | 說明 | OpenClaw 實證 |
|------|------|--------------|
| **代理隔離** | 每個代理擁有獨立的工作空間、記憶體、Session | `src/agents/agent-scope.ts` — `ResolvedAgentConfig` |
| **事件驅動** | 所有代理間通訊透過 Message Bus | `src/infra/agent-events.ts` — Pub/Sub + 單調遞增序號 |
| **插件化擴展** | 新代理/工具以插件形式熱插拔 | `src/plugins/` — 4 來源發現 + 10+ 註冊方法 |
| **防禦縱深** | Docker 沙盒 + Rate Limit + RBAC | `src/agents/sandbox/` — 容器化隔離 |
| **(新增) API-First 資料取得** | 優先使用合法 API 取得 SERP/SEO 數據，自建爬蟲僅為最末備援 | *GSC API → SERP SaaS API → 自建 Playwright（限低頻場景）* |
| **(新增) MVP 精簡儲存** | MVP 僅 PostgreSQL+pgvector+Redis 三系統；TimescaleDB 待資料量超千萬行再啟用 | *OpenClaw 原生 SQLite 不足，改用 PG+pgvector* |
| **分層配置** | 全局 → 代理群組 → 個別代理 | `src/config/validation.ts` — 分層 Defaults |
| **持久化恢復** | 任務狀態持久化，重啟後自動恢復 | `src/agents/subagent-registry.store.ts` |

---

## 3. 6 Smart Agents + 6 Auto Tasks 定義

> **設計原則**：所有 12 個單元共用同一套 Agent Runtime（workspace 隔離、事件發送、cron 排程），但依據「是否需要 LLM 推理循環」分為兩層：
> - **🧠 Smart Agent**：需要 LLM 進行創意生成、語意分析或策略推理
> - **⚙️ Auto Task**：確定性管線（API 呼叫 → 資料轉換 → 儲存），不啟動 LLM 推理循環，`model: null`
>
> Auto Tasks 可在最終輸出時「可選」呼叫 cheapModel 生成人類可讀摘要，但核心邏輯不依賴 LLM。

### 3.1 代理總覽表

| # | 類型 | 代理名稱 | 代號 | 職責 | LLM 模型 | 執行頻率 |
|---|------|---------|------|------|---------|--------|
| 1 | 🧠 Smart | 關鍵字研究代理 | `keyword-researcher` | 關鍵字挖掘、意圖分類、難度評估 | smartModel (Claude Opus 4) | 按需 |
| 2 | 🧠 Smart | 內容寫手代理 | `content-writer` | SEO 文章生成、Meta 標籤優化 | smartModel (Claude Opus 4) | 按需 |
| 3 | ⚙️ Auto | 技術審計代理 | `technical-auditor` | 網站爬蟲、技術 SEO 問題檢測 | `null`（可選 cheapModel 摘要） | 每週 |
| 4 | 🧠 Smart | 反向連結代理 | `backlink-builder` | 外連機會發現、Outreach 草稿 | smartModel (Claude Sonnet 4) | 每週 |
| 5 | 🧠 Smart | 競爭監控代理 | `competitor-monitor` | 競品內容追蹤、SERP 變化分析 | smartModel (Claude Sonnet 4) | 每日 |
| 6 | ⚙️ Auto | Local SEO 代理 | `local-seo` | GMB 管理、Local Citation 檢查 | `null`（可選 cheapModel 摘要） | 每週 |
| 7 | 🧠 Smart | 報告生成代理 | `report-generator` | 整合數據、自動產出報告 | smartModel (GPT-4o) | 每週/每月 |
| 8 | ⚙️ Auto | Schema Markup 代理 | `schema-agent` | 結構化資料生成與驗證 | `null` | 按需 |
| 9 | ⚙️ Auto | 內部連結代理 | `internal-linker` | 內連結構最佳化、孤兒頁面偵測 | `null`（可選 cheapModel 建議） | 每週 |
| 10 | ⚙️ Auto | 頁面速度代理 | `pagespeed-agent` | Core Web Vitals 監控與優化建議 | `null`（可選 cheapModel 摘要） | 每日 |
| 11 | ⚙️ Auto | SERP 追蹤代理 | `serp-tracker` | 每日關鍵字排名追蹤 | `null` | 每日 |
| 12 | 🧠 Smart | 內容更新代理 | `content-refresher` | 舊文章偵測 + 自動改寫更新 | smartModel (Claude Opus 4) | 每月 |

> **LLM 模型路由**：透過 **Vercel AI SDK** 統一抽象層管理多 provider（Anthropic/OpenAI/Google），不自行維護兩套 SDK。
> 各代理使用 `smartModel`（策略推理，如 Claude Opus 4 / GPT-4o）或 `cheapModel`（簡單摘要，如 GPT-4o-mini / Claude Haiku）。
> Auto Tasks 核心邏輯不使用 LLM，僅在輸出層可選使用 cheapModel。

### 3.2 各代理詳細規格

#### Agent 1: 關鍵字研究代理 (`keyword-researcher`)

```yaml
id: keyword-researcher
name: "Keyword Research Agent"
description: "端到端關鍵字研究流水線"

工作空間: ~/seo-platform/agents/keyword-researcher/
記憶體: 獨立向量庫（歷史關鍵字 + 搜尋數據）

輸入:
  - 種子關鍵字（手動 or 來自 competitor-monitor）
  - 目標市場/語言/地區
  - 客戶產業分類

處理流程:
  1. Google Suggest API 擴展長尾關鍵字
  2. 從 Ahrefs/SEMrush API 獲取搜尋量 + 難度
  3. NLP 分析搜尋意圖（Informational/Navigational/Transactional/Commercial）
  4. 群組化為 Topic Clusters
  5. 計算「速贏分數」(Quick Win Score = 高搜尋量 × 低競爭度)
  6. 與 Memory 中的歷史數據交叉比對

輸出:
  - JSON: 完整關鍵字矩陣
  - Excel/CSV: 可下載的關鍵字報告
  - 內部事件: 將高優先關鍵字推送給 content-writer

工具:
  - google-suggest-scraper（Browser Control）
  - ahrefs-api-client
  - semrush-api-client
  - nlp-intent-classifier（本地模型）
  - keyword-clustering（TF-IDF + LDA）

LLM 用途:
  - 搜尋意圖判斷
  - 主題群組命名
  - 機會分析摘要

LLM 步驟級路由:
  - 步驟 1-2: 無 LLM（API 呼叫 + 數據取得）
  - 步驟 3: smartModel（意圖分類需語意理解）
  - 步驟 4: cheapModel（群組命名可用輕量模型）
  - 步驟 5-6: 無 LLM（數字計算 + DB 查詢）
  - 最終摘要: smartModel（機會分析需深度推理）

排程: 按需觸發 + 每月自動探索新機會
沙盒: 啟用（Docker，限制網路白名單）
```

#### Agent 2: 內容寫手代理 (`content-writer`)

```yaml
id: content-writer
name: "Content Writer Agent"
description: "SEO 優化的內容生成引擎"

工作空間: ~/seo-platform/agents/content-writer/
記憶體: 品牌風格指南 + 歷史文章 + 關鍵字計畫

輸入:
  - 目標關鍵字（來自 keyword-researcher）
  - 競品參考文章（來自 competitor-monitor）
  - 品牌語調指南 (SOUL.md)
  - 內部連結建議（來自 internal-linker）

處理流程:
  1. 分析 SERP Top 10 內容結構
  2. 生成文章大綱（H1/H2/H3 結構）
  3. 撰寫完整文章（含 LSI 關鍵字）
  4. 生成 Meta Title / Description / OG Tags
  5. 插入 Schema Markup 建議（來自 schema-agent）
  6. 內部連結自動添加
  7. SEO 評分自評（Readability + Keyword Density）

輸出:
  - Markdown: 完整文章草稿
  - JSON: Meta 標籤 + Schema Markup
  - 內部事件: 通知 report-generator 記錄內容生產量

品質控制:
  - Flesch-Kincaid 可讀性評分 ≥ 60
  - 關鍵字密度 1.5% - 2.5%
  - 自動交叉檢查避免重複內容（與 Memory 中歷史文章比對）

LLM 用途:
  - 文章全文生成
  - 標題優化（A/B 測試版本）
  - SEO 評分解釋

LLM 步驟級路由:
  - 步驟 1: cheapModel（SERP 結構分析可用輕量模型）
  - 步驟 2: smartModel（大綱生成需策略推理）
  - 步驟 3: smartModel（全文寫作是核心价值）
  - 步驟 4: cheapModel（Meta 標籤為模板式任務）
  - 步驟 5-6: 無 LLM（規則式插入）
  - 步驟 7: cheapModel（數字評分不需深度推理）

排程: 按需觸發（由 keyword-researcher 的 Quick Win 清單驅動）
沙盒: 不啟用（純文字生成，無需系統權限）
```

#### Agent 3: 技術審計代理 (`technical-auditor`) — ⚙️ Auto Task

```yaml
id: technical-auditor
name: "Technical SEO Audit Agent"
type: auto-task          # 確定性管線，不需 LLM 推理
model: null              # 核心邏輯為規則式檢查，可選 cheapModel 生成摘要
description: "全站技術健康檢查引擎"

工作空間: ~/seo-platform/agents/technical-auditor/
記憶體: 歷史審計結果 + 修復紀錄

輸入:
  - 目標網站 URL（sitemap.xml 自動發現）
  - 審計深度設定（Quick / Standard / Deep）
  - 上次審計結果（差異比較）

處理流程:
  1. 解析 sitemap.xml + robots.txt
  2. 全站爬蟲（Headless Chrome in Sandbox）
     - HTTP 狀態碼檢查（200/301/302/404/500）
     - 回應時間記錄
     - 重導鏈偵測
  3. On-Page 檢查（每頁）：
     - Title 長度（30-60 字元）
     - Meta Description 長度（120-160 字元）
     - H1 唯一性
     - Image Alt Text 完整性
     - Canonical Tag 正確性
     - hreflang 標籤
     - Open Graph Tags
  4. 站點結構分析：
     - 頁面深度分布（理想 ≤ 3 層）
     - 內部連結斷裂偵測
     - 孤兒頁面發現
  5. Core Web Vitals 快照（呼叫 pagespeed-agent）
  6. Mobile-Friendly 測試
  7. HTTPS/SSL 檢查
  8. 與上次審計結果差異比較

輸出:
  - JSON: 完整審計數據（結構化）
  - HTML/PDF: 審計報告（含嚴重/警告/資訊分級）
  - 修復優先清單（按影響程度排序）
  - 內部事件: 嚴重問題即時推送

工具:
  - headless-chrome-crawler（Browser Control + CDP）
  - lighthouse-api
  - sitemap-parser
  - ssl-checker
  - robots-txt-parser

排程: 每週日 02:00 自動執行
沙盒: 啟用（Docker + 網路白名單）
併發: 最大 5 個同時爬取的頁面（避免打掛目標網站）
```

#### Agent 4: 反向連結代理 (`backlink-builder`)

```yaml
id: backlink-builder
name: "Backlink Builder Agent"
description: "外連機會發現與 Outreach 自動化"

處理流程:
  1. 分析現有 Backlink Profile（Ahrefs API）
  2. 研究競爭對手 Backlinks，找出我方未有的高權重來源
  3. Broken Link Building: 發現目標網站的 404 頁面
  4. Guest Post 機會搜尋（"write for us" + 產業關鍵字）
  5. 生成個人化 Outreach Email 草稿（⚠️ 僅生成草稿，不自動發送）
  6. 追蹤已發出的 Outreach 狀態

LLM 步驟級路由:
  - 步驟 1-4: 無 LLM（API 查詢 + 規則式篩選）
  - 步驟 5: smartModel（個人化 Email 需創意寫作）
  - 步驟 6: 無 LLM（狀態追蹤為 DB 查詢）

輸出:
  - CSV: Backlink 機會清單（含 DA/PA 評分）
  - Markdown: Outreach Email 草稿模板
  - 內部事件: 新高價值機會通知

⚠️ 安全限制:
  - 所有 Email 僅生成草稿存於本地
  - 每日 Outreach 數量上限: 10 封
  - 必須經由人工審核後才可發送

排程: 每週一 08:00 執行
```

#### Agent 5: 競爭監控代理 (`competitor-monitor`) — 🧠 Smart Agent

```yaml
id: competitor-monitor
name: "Competitor Monitor Agent"
type: smart-agent         # 需要 LLM 進行競品策略分析
model: smartModel         # Claude Sonnet 4（從 GPT-4o-mini 升級：競品分析需較強推理能力）
description: "24/7 競爭對手動態監控"

處理流程:
  1. **（API-First）** 透過 Ahrefs/SEMrush API 取得競品新增/變更頁面清單
  2. **（API-First）** 透過 Ahrefs API 偵測競品新增的 Backlinks
  3. **（API-First）** 透過 ValueSERP/Scale SERP 監控指定關鍵字 SERP 前 20 變化
  4. **（補充爬取）** 僅對 API 無法覆蓋的內容（如競品 Blog 全文），低頻爬取首頁 + Blog
     ⚠️ 若競品有反爬機制（Cloudflare 等），跳過並標記為 "需人工檢查"
  5. 使用 Diff 引擎偵測內容變化
  6. 分析競品新增/修改頁面的 SEO 策略（LLM smartModel）

LLM 步驟級路由:
  - 步驟 1-5: 無 LLM（API 呼叫 + Diff 計算）
  - 步驟 6: smartModel（策略分析需推理能力）
  - 每日摘要生成: cheapModel（摘要不需深度推理）

輸出:
  - 每日摘要: "競爭對手 XXX 新增 3 篇文章，主攻關鍵字 YYY"
  - 每週趨勢報告: 競品 Content Velocity 對比
  - 即時警報: 我方排名被超越時 Slack/Email 通知

排程: 每日 06:00 + 即時警報
沙盒: 啟用
```

#### Agent 6: Local SEO 代理 (`local-seo`) — ⚙️ Auto Task

```yaml
id: local-seo
name: "Local SEO Agent"
type: auto-task           # API 查詢 + 規則式檢查，不需 LLM
model: null              # 步驟 2 可選 cheapModel 生成 Review 回應草稿
description: "本地搜尋優化專家"

處理流程:
  1. 檢查 Google Business Profile 完整度
  2. 監控並生成 Review 回應草稿（cheapModel, best-effort）
  3. NAP (Name/Address/Phone) 一致性檢查
  4. Local Citation 建設機會發現
  5. 生成 LocalBusiness Schema Markup

⚠️ Review 回應草稿品質聲明:
  - 步驟 2 使用 cheapModel 生成，品質為 best-effort
  - 不保證語調一致性，建議人工審閱後再發布
  - 若團隊需要高品質 Review 回應，應升級為 Smart Agent 並使用 smartModel

排程: 每週三 10:00
```

#### Agent 7: 報告生成代理 (`report-generator`)

```yaml
id: report-generator
name: "Report Generator Agent"
description: "多源數據整合 + 自動報告"

處理流程:
  1. 從 GA4 API 抓取流量數據
  2. 從 Search Console API 抓取搜尋表現
  3. 從其他 Agent 的 Memory 中收集執行成果
  4. 計算 KPI 變化（對比上期/去年同期）
  5. 生成圖表（Plotly / Chart.js JSON）
  6. 撰寫 Executive Summary（LLM）
  7. 輸出 PDF 報告

LLM 步驟級路由:
  - 步驟 1-5: 無 LLM（API 呼叫 + 數字計算 + 圖表生成）
  - 步驟 6: smartModel（Executive Summary 需綜合分析能力）
  - 步驟 7: 無 LLM（PDF 模板渲染）
  - 每日簡報: cheapModel（簡短摘要不需深度推理）

輸出:
  - PDF: 完整 SEO 報告
  - JSON: 原始數據 + 圖表配置
  - Slack 通知: 報告摘要 + 下載連結

排程:
  - 每日簡報: 08:00（關鍵指標快照）
  - 每週報告: 週一 09:00
  - 每月報告: 1 日 09:00
```

#### Agent 8: Schema Markup 代理 (`schema-agent`) — ⚙️ Auto Task

```yaml
id: schema-agent
name: "Schema Markup Agent"
type: auto-task           # 規則式 JSON-LD 生成 + API 驗證，不需 LLM
model: null
description: "結構化資料生成與驗證"

處理流程:
  1. 分析頁面內容類型（Article/Product/FAQ/Recipe...）
  2. 生成對應的 JSON-LD Schema Markup
  3. 透過 Google Rich Results Test API 驗證
  4. 偵測現有 Schema 的錯誤/警告

輸出:
  - JSON-LD: 已驗證的 Schema Markup
  - 錯誤報告: 現有 Schema 問題清單

排程: 按需（由 content-writer 觸發）+ 每月全站掃描
```

#### Agent 9: 內部連結代理 (`internal-linker`) — ⚙️ Auto Task

```yaml
id: internal-linker
name: "Internal Linking Agent"
type: auto-task           # 圖論計算 + 規則式建議，不需 LLM
model: null              # 可選 cheapModel 生成 Anchor Text 建議
description: "內連結構最佳化引擎"

處理流程:
  1. 全站內連結圖譜建立（Graph 數據結構）
  2. 計算每個頁面的 Internal PageRank
  3. 偵測孤兒頁面（0 個 Internal Links 指向）
  4. 建議新增內連結（語意相關性匹配）
  5. 偵測 Anchor Text 過度優化
  6. 建議 Pillar → Cluster 連結關係

輸出:
  - Mermaid 圖表: 站點內連結構
  - CSV: Orphan Pages 清單
  - JSON: 內連結建議清單（含 Anchor Text 推薦）

排程: 每週六 03:00（避開流量高峰）
```

#### Agent 10: 頁面速度代理 (`pagespeed-agent`) — ⚙️ Auto Task

```yaml
id: pagespeed-agent
name: "PageSpeed Agent"
type: auto-task           # 純 API 呼叫 + 數字比對，不需 LLM
model: null              # 可選 cheapModel 生成優化建議摘要
description: "Core Web Vitals 監控與優化"

處理流程:
  1. 呼叫 Google PageSpeed Insights API
  2. 記錄 LCP / FID / CLS / TTFB / INP
  3. 與歷史數據對比趨勢
  4. 分析效能瓶頸（圖片/JS/CSS/Server）
  5. 生成具體優化建議

輸出:
  - 每日趨勢圖: Core Web Vitals 變化
  - 優化清單: 按影響程度排序
  - 即時警報: CWV 超過閾值時通知

排程: 每日 05:00
```

#### Agent 11: SERP 追蹤代理 (`serp-tracker`) — ⚙️ Auto Task

```yaml
id: serp-tracker
name: "SERP Tracker Agent"
type: auto-task           # 確定性 API 呼叫 + 資料儲存，不需 LLM
model: null
description: "關鍵字排名每日監控"

處理流程:
  1. 讀取目標關鍵字清單（來自 keyword-researcher Memory）
  2. **API-First 策略取得排名資料**：
     - L1: Google Search Console API（合法、免費）
     - L2: ValueSERP / Scale SERP API（付費 SaaS，風險轉嫁）
     - L3: 自建 Playwright + Proxy（僅用於 L1+L2 無法覆蓋的極低頻場景）
  3. 解析排名位置、Featured Snippet、People Also Ask
  4. 記錄歷史排名數據（PostgreSQL 時序資料）
  5. 偵測排名劇烈變動（上升/下降 >5 位）

輸出:
  - 時序數據: 每日排名快照
  - 即時警報: 排名劇烈變動
  - 每週趨勢圖: Top 50 關鍵字走勢

排程: 每日 07:00
沙盒: 啟用（使用 Proxy Pool 防封）
```

#### Agent 12: 內容更新代理 (`content-refresher`)

```yaml
id: content-refresher
name: "Content Refresher Agent"
description: "舊文章偵測與自動更新"

處理流程:
  1. 掃描所有已發布文章的 Last Modified 日期
  2. 識別超過 90 天未更新 + 流量下降的文章
  3. 分析當前 SERP 上方競品的內容變化
  4. 生成更新建議（新增段落/更新數據/改善結構）
  5. 呼叫 content-writer 進行改寫
  6. 提交為草稿等待人工審核

LLM 步驟級路由:
  - 步驟 1-2: 無 LLM（DB 查詢 + GA4 API 流量比對）
  - 步驟 3: cheapModel（競品內容摘要可用輕量模型）
  - 步驟 4: smartModel（更新策略需綜合分析）
  - 步驟 5: smartModel（委派 content-writer 執行改寫）
  - 步驟 6: 無 LLM（狀態變更）

輸出:
  - CSV: 需要更新的文章清單（含優先度）
  - Markdown: 更新後的文章草稿
  - 對比視圖: 舊版 vs 新版 Diff

排程: 每月 1 日 10:00
```

---

## 4. 核心模組技術規格

### 4.1 Orchestrator（BullMQ Flow 工作流引擎）

**借鑑自：** OpenClaw `src/gateway/server.impl.ts` (事件) + BullMQ Flow (工作流)

> **設計決策**：選定 **BullMQ Flow** 為唯一工作流引擎。
> - Event Bus 降級為純通知/日誌層，不承擔業務邏輯。
> - 選型歷史與排除方案見「修正 2」。

```typescript
// BullMQ Flow 工作流定義
import { FlowProducer, Queue, Worker } from 'bullmq';

// 工作流引擎（基於 BullMQ Flow parent-child dependency）
interface SEOWorkflowEngine {
  // 啟動一個長流程任務 (e.g. 全站關鍵字審核)
  startFlow(flowName: string, input: any): Promise<FlowJobId>;
  
  // 查詢當前狀態 (Step 2 of 5: Waiting for Content Writer)
  getStatus(flowId: string): FlowState;
  
  // 人工介入完成 (Approval/Reject)
  signalJob(jobId: string, signal: "approve" | "reject", data: any): Promise<void>;
  
  // 失敗處理：自動重試 + dead-letter + 通知人工
  // 失敗處理：見修正 2（不採用 Saga，改用 failed + 人工通知）
}

// 任務定義 (Task Definition)
interface Task {
  id: string;                        // UUID
  type: TaskType;                    // enum
  priority: "critical" | "high" | "medium" | "low";
  state: "created" | "running" | "paused" | "completed" | "failed";
  retries: number;
  maxRetries: number;                // 預設 3
  backoff: { type: 'exponential'; delay: 5000 };
}

// BullMQ Flow 範例：關鍵字 → 內容 → 連結 → 報告
const seoContentFlow = {
  name: 'seo-content-pipeline',
  queueName: 'orchestrator',
  children: [
    {
      name: 'keyword-research',
      queueName: 'smart-agents',
      children: [
        { name: 'content-writing', queueName: 'smart-agents' },
        { name: 'schema-generation', queueName: 'auto-tasks' },
        { name: 'internal-linking', queueName: 'auto-tasks' },
      ]
    }
  ]
};
```

**架構示意：**

```
User Request ──(API)──> Orchestrator (BullMQ Flow) ──(Job)──> Agent Worker
                             │ Redis + PostgreSQL 持久化
                             ▼
                        Dead-Letter Queue → Slack 通知人工介入
```

**Event Bus（降級為通知/日誌層 + Outbox 可靠投遞）：**
> Event Bus 僅用於：① 代理執行日誌廣播 ② Dashboard 即時更新 ③ Slack/Email 通知觸發。
> 所有業務邏輯（任務依賴、重試、優先序）由 BullMQ Flow 管理。

**Outbox Pattern（確保事件投遞一致性）：**
```typescript
// 在同一個 DB transaction 中寫入業務數據 + 事件
async function completeTaskWithEvent(taskId: string, result: TaskResult) {
  await db.transaction(async (tx) => {
    // 1. 更新業務狀態
    await tx.update(tasks).set({ state: 'completed', result }).where(eq(tasks.id, taskId));
    
    // 2. 同步寫入 Outbox（與業務寫入同一 transaction，保證原子性）
    await tx.insert(eventsOutbox).values({
      eventType: 'task.completed',
      payload: { taskId, result },
      dispatched: false,
    });
  });
  // Dispatcher 獨立輪詢 outbox，投遞到 WebSocket/Slack/Email
}
```

```sql
-- Outbox 事件表
CREATE TABLE events_outbox (
  id BIGSERIAL PRIMARY KEY,
  event_type TEXT NOT NULL,             -- 'task.completed', 'rank.alert', ...
  payload JSONB NOT NULL,
  dispatched BOOLEAN DEFAULT false,
  dispatched_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_outbox_pending ON events_outbox(dispatched) WHERE dispatched = false;
```
> **Dispatcher** 獨立進程每 1-5 秒輪詢 `events_outbox`，投遞成功後標記 `dispatched = true`。
> 若投遞失敗，自動重試（指數退避），超過 N 次寫入 dead-letter 並通知運維。

**任務依賴解析（DAG 模式範例）：**

```
keyword-researcher ──→ content-writer ──→ internal-linker
       │                     │                   │
       │                     ▼                   ▼
       │              schema-agent         report-generator
       ▼
competitor-monitor ──→ backlink-builder
       │
       ▼
serp-tracker ──→ pagespeed-agent ──→ report-generator
```

### 4.2 Agent Runtime（代理執行環境）

**借鑑自：** OpenClaw `src/agents/agent-scope.ts` + `src/agents/sandbox/`

```typescript
interface AgentRuntime {
  id: AgentId;
  type: 'smart-agent' | 'auto-task'; // 兩層分類
  workspace: string;               // 隔離的工作目錄
  memory: VectorStore;             // 專屬向量搜尋
  tools: Tool[];                   // 可用工具列表
  model: ModelConfig | null;       // Auto Task 設為 null
  sandbox: SandboxConfig;          // 沙盒設定
  systemPrompt: string;            // 專業 Prompt（Smart Agent 用）
  session: SessionManager;         // 會話管理
}

interface AgentConfig {
  id: string;
  name: string;
  type: 'smart-agent' | 'auto-task';
  workspace: string;
  model: {
    // Smart Agent: 使用 Vercel AI SDK 統一路由
    smartModel?: string;          // "anthropic/claude-opus-4"（需 LLM 推理時）
    cheapModel?: string;          // "openai/gpt-4o-mini"（摘要/簡單任務）
    fallback?: string;            // 備用模型
    maxTokens: number;
    temperature: number;
  } | null;                        // Auto Task 設為 null
  sandbox: {
    enabled: boolean;
    docker: {
      image: string;
      network: "none" | "bridge";  // bridge 用於需要網路的代理
      memory: string;              // "512m"
      cpus: string;                // "1.0"
      capDrop: string[];           // ["ALL"]
    };
  };
  schedule?: {
    cron: string;                  // "0 8 * * *"
    timezone: string;              // "Asia/Taipei"
  };
  tools: string[];                 // 允許使用的工具 ID
  permissions: {
    network: string[];             // 允許訪問的域名
    fileSystem: "read-only" | "read-write";
    maxConcurrency: number;
  };
}
```

### 4.3 Browser Engine（爬蟲引擎 — API-First 策略）

**基礎架構：** OpenClaw `src/browser/` (Playwright) + **外部 SERP SaaS API**

> **設計決策**：SERP 資料取得採用三級 API-First 策略，自建爬蟲僅為最末備援。
> 技術審計的全站爬取（`technical-auditor`）因為是爬取客戶自有網站，不涉及反爬問題，保留 Playwright 直接爬取。

```typescript
// 三級 SERP 資料來源策略
interface SERPDataStrategy {
  // L1: 合法、免費/低成本（優先使用）
  l1_official: {
    googleSearchConsole: GSCClient;    // 官方 API，合法，免費
    googleAnalytics4: GA4Client;       // 官方 API，合法，免費
  };

  // L2: 付費 SaaS API（風險轉嫁給 SaaS 供應商）
  l2_saas: {
    valueSERP: ValueSERPClient;        // $50-100/月，含 SERP 解析
    scaleSERP: ScaleSERPClient;        // 備選 SERP API
    ahrefsAPI: AhrefsClient;          // 關鍵字數據、Backlink 分析
    semrushAPI: SEMrushClient;         // 關鍵字數據、競品分析
  };

  // L3: 自建 Playwright 爬取（僅用於 L1+L2 無法覆蓋的極低頻場景）
  l3_selfCrawl: {
    playwright: PlaywrightEngine;      // 基於 OpenClaw src/browser/
    proxyPool?: ProxyConfig;           // 可選 Proxy 輪替
    maxFrequency: '10_queries_per_day'; // 嚴格限頻
  };
}

// 技術審計專用爬蟲（爬取客戶自有網站，無反爬問題）
interface SiteCrawler {
  crawlSite(sitemapUrl: string, options: CrawlOptions): AsyncGenerator<CrawlResult>;
  navigate(url: string): Promise<PageSnapshot>;
  screenshot(selector?: string): Promise<Buffer>;
  extractText(selector: string): Promise<string>;
}

interface CrawlOptions {
  maxPages: number;                   // 最大爬取頁數
  maxDepth: number;                   // 最大深度
  concurrency: number;                // 並行數（建議 3-5）
  delayMs: number;                    // 請求間隔（建議 1000-3000）
  respectRobotsTxt: boolean;          // 遵守 robots.txt
  allowedDomains: string[];           // 域名白名單
  excludePatterns: RegExp[];          // 排除 URL 模式
}
```

**成本估算對比：**
| 方案 | 月成本 | 法律風險 | 維護成本 |
|------|--------|---------|---------|
| GSC + GA4 API | 免費 | 無 | 低 |
| ValueSERP (5K 次/月) | $50 | 低（風險轉嫁） | 低 |
| Ahrefs API (Lite) | $99 | 低 | 低 |
| 自建 Playwright + Proxy | $200-500+ | 高（Google ToS） | 高 |

### 4.4 Memory & Vector Store（精簡儲存架構）

**借鑑自：** OpenClaw `src/memory/manager.ts` (概念) + PostgreSQL + pgvector (實作)

> **設計決策（MVP 精簡）**：僅使用 **PostgreSQL + pgvector + Redis** 三個儲存系統。
> - **不引入 TimescaleDB**（MVP 階段）：排名歷史直接存 PG 分區表，升級路徑見 **8.3 Scale-up Playbook**。
> - **不引入 MinIO**：文件儲存用本地 FS 或 S3，MVP 不需要自建物件儲存。

**三系統分工：**
1.  **Redis (L1)：** 短期對話上下文、BullMQ 任務佇列、快取熱點資料
2.  **PostgreSQL + pgvector (L2)：** 關鍵字矩陣、審計結果、排名歷史（分區表）、向量搜尋（語意檢索）
3.  **升級路徑：** 見 8.3 Scale-up Playbook（TimescaleDB / S3 / 讀寫分離）

```typescript
// 統一儲存介面 (Storage Abstraction Layer)
interface SEOMemoryStore {
  // L2: 向量檢索 (Semantic Search via pgvector)
  searchSimilarKeywords(vector: number[], limit: number): Promise<Keyword[]>;
  findRelatedArticles(topicVector: number[]): Promise<Article[]>;

  // L2: 時間序列（PostgreSQL 分區表，升級路徑見 8.3）
  recordDailyRank(keywordId: string, rank: number, date: Date): Promise<void>;
  getRankHistory(keywordId: string, range: DateRange): Promise<RankPoint[]>;

  // L2: 結構化數據 (Audit Data)
  saveAuditResult(auditId: string, issues: TechnicalIssue[]): Promise<void>;
}
```

**數據遷移策略：**
- **初期開發（MVP）：** PostgreSQL + pgvector + Redis（3 系統）。
- **升級路徑：** 見 8.3 Scale-up Playbook（TimescaleDB / S3 / 讀寫分離）。
- **檔案儲存：** 報告 PDF/CSV 存本地 FS 或 S3，不引入 MinIO。

  // 混合搜尋（BM25 + Vector）
  search(query: string, options?: SearchOptions): Promise<SearchResult[]>;

  // SEO 專用查詢
  getKeywordHistory(keyword: string): Promise<KeywordTimeline>;
  getRankHistory(keyword: string, domain: string): Promise<RankTimeline>;
  getContentByTopic(topic: string): Promise<ContentDoc[]>;
  getCompetitorChanges(domain: string, since: Date): Promise<ChangeEntry[]>;

  // 索引管理
  sync(): Promise<void>;
  status(): Promise<MemoryStatus>;
}

interface SEODocument {
  id: string;
  type: "keyword" | "content" | "audit" | "rank" | "backlink" | "competitor";
  title: string;
  content: string;                    // 用於向量化
  metadata: Record<string, unknown>;  // 結構化數據
  timestamp: number;
}
```

### 4.5 Notification Hub（通知中心）

```typescript
interface NotificationHub {
  // 多通道通知
  send(notification: Notification): Promise<void>;

  channels: {
    slack: SlackNotifier;             // Webhook integration
    email: EmailNotifier;             // SMTP（僅系統通知）
    telegram: TelegramNotifier;       // Bot API
    webhook: GenericWebhookNotifier;  // Custom endpoints
    dashboard: DashboardNotifier;     // WebSocket push
  };

  // 通知規則
  rules: NotificationRule[];          // 條件觸發
  // 例: rank_drop > 5 positions → Slack #seo-alerts → priority: high
}
```

---

## 5. 數據流與代理協作機制

### 5.1 完整 SEO 工作流程（端到端）

```
Phase 1: 研究 (Research)
──────────────────────────────────────────────
[人類] 輸入種子關鍵字 + 目標市場
    │
    ▼
[keyword-researcher] → 產出關鍵字矩陣
    │                    │
    │                    ▼ 事件: keyword_matrix_ready
    ▼
[competitor-monitor] → 分析競品對該組關鍵字的佈局
    │
    ▼ 事件: competitor_analysis_ready

Phase 2: 規劃 (Planning)
──────────────────────────────────────────────
[Orchestrator] 綜合研究結果，生成「內容行事曆」
    │
    ▼ 事件: content_calendar_created
[人類] 審核內容行事曆 → 批准 / 調整
    │
    ▼ 事件: calendar_approved

Phase 3: 生產 (Production)
──────────────────────────────────────────────
[content-writer] ← 接收關鍵字 + 競品分析
    │  同時請求:
    │  ← [schema-agent] 提供 Schema Markup
    │  ← [internal-linker] 提供內連結建議
    │
    ▼ 事件: draft_ready
[人類] 審核文章草稿 → 批准 / 退回修改
    │
    ▼ 事件: content_approved

Phase 4: 發布 (Publishing)
──────────────────────────────────────────────
[Orchestrator] 執行發布任務:
    → WordPress API: 發布文章
    → [schema-agent]: 驗證 Schema
    → [internal-linker]: 在相關舊文章中加入內連
    ▼ 事件: content_published

Phase 5: 監控 (Monitoring)
──────────────────────────────────────────────
[serp-tracker] 每日追蹤新發布頁面排名
    ▼ 事件: rank_update

[pagespeed-agent] 監控發布頁面的 CWV
    ▼ 事件: cwv_update

[competitor-monitor] 監控競品回應動態
    ▼ 事件: competitor_reaction

Phase 6: 報告 (Reporting)
──────────────────────────────────────────────
[report-generator] 定時整合所有數據
    → GA4 API 流量數據
    → GSC API 搜尋表現
    → 各 Agent Memory 執行摘要
    │
    ▼ 生成 PDF 報告 + Slack 通知
```

### 5.2 代理間通訊協議

**借鑑自：** OpenClaw `src/infra/agent-events.ts` — 事件驅動 + 單調遞增序號

```typescript
interface AgentMessage {
  id: string;                         // UUID
  seq: number;                        // 單調遞增序號（防亂序）
  from: AgentId;                      // 發送代理
  to: AgentId | "orchestrator";       // 目標代理
  type: "request" | "response" | "event" | "notification";
  payload: {
    taskId?: string;
    data: Record<string, unknown>;
  };
  timestamp: number;
  ttl: number;                        // 消息有效期（秒）
}
```

### 5.3 Human-in-the-Loop 審核機制

```typescript
interface ApprovalGate {
  id: string;
  taskId: string;
  agentId: AgentId;
  type: "content-publish" | "email-send" | "config-change" | "high-value-action";
  content: {
    summary: string;                  // 人類可讀摘要
    preview: string;                  // 預覽內容（HTML/Markdown）
    diff?: string;                    // 變更對比
    riskLevel: "low" | "medium" | "high";
  };
  deadline: number;                   // 超時自動拒絕
  status: "pending" | "approved" | "rejected" | "expired";

  // 審核渠道
  notifyVia: ("dashboard" | "slack" | "email")[];

  // 審核結果
  reviewedBy?: string;
  reviewedAt?: number;
  feedback?: string;
}
```

---

## 6. 從 OpenClaw 借鑑的架構模式

### 6.1 可直接複用的核心模式

| # | 架構模式 | OpenClaw 原始碼 | 如何適配 SEO 平台 |
|---|---------|----------------|------------------|
| 1 | **Gateway 啟動序列** | `server.impl.ts` — 載入配置 → 驗證 → 初始化子系統 → 掛載 Handler | BullMQ Flow Orchestrator 採用相同順序啟動 12 個代理 |
| 2 | **Multi-Agent 隔離** | `agent-scope.ts` — 每個 Agent 有獨立 workspace + sessions | 每個 SEO Agent 完全隔離 |
| 3 | **Binding 路由** | `bindings.ts` — Most-Specific-Wins 路由器 | 任務路由到最適合的代理 |
| 4 | **Plugin Discovery** | `discovery.ts` — 4 來源掃描 | SEO 工具插件自動發現 |
| 5 | **Plugin API** | `types.ts` — 10+ register 方法 | 第三方 SEO 工具插件介面 |
| 6 | **Zod Config Validation** | `zod-schema.ts` + `validation.ts` | 全局配置 + 代理配置驗證 |
| 7 | **事件系統** | `agent-events.ts` — Pub/Sub + 單調序號 | 代理間消息通訊 |
| 8 | **子代理生命週期** | `subagent-registry.ts` — Spawn → Execute → Wait → Announce → Cleanup | 代理間委派任務 |
| 9 | **Cron 調度** | `cron/service.ts` — 持久化 + 隔離執行 | 12 個代理的排程管理 |
| 10 | **Docker Sandbox** | `sandbox/config.ts` — 分層配置 + 容器化 | 爬蟲代理安全隔離 |
| 11 | **Browser CDP** | `browser/cdp.ts` + `pw-tools-core.ts` | SERP 爬取 + 技術審計 |
| 12 | **混合搜尋** | `memory/manager.ts` — BM25 + Vector | SEO 知識庫全文 + 語意搜尋 |
| 13 | **Webhook 系統** | `hooks.ts` — Token 驗證 + Rate Limit | 外部整合 + 回調 |
| 14 | **分層配置** | `validation.ts` — Global → Agent → Override | 方便多租戶管理 |
| 15 | **優雅關閉** | `server-close.ts` — 有序關閉子系統 | 確保數據不丟失 |

### 6.2 需要全新開發的模組

| 模組 | 原因 |
|------|------|
| **SEO Dashboard** | OpenClaw 使用 Lit 做控制台；SEO 平台需要 React/Next.js + 圖表 |
| **排名歷史儲存** | PG 分區表（升級路徑見 8.3 Scale-up Playbook） |
| **SERP 資料層** | API-First 三級策略（GSC → SaaS API → 自建爬蟲備援） |
| **PDF 報告引擎** | OpenClaw 無 PDF 報告生成 |
| **CMS 整合層** | WordPress/Shopify API 整合 |
| **多租戶 RBAC** | OpenClaw 是單用戶系統 |
| **LLM Gateway** | Vercel AI SDK 統一多 provider 抽象層 |

---

## 7. 技術選型與堆疊

### 7.1 後端

| 層級 | 技術 | 原因 |
|------|------|------|
| **Runtime** | Node.js 22+ (TypeScript 5.7) | 與 OpenClaw 一致，複用最大化 |
| **HTTP Framework** | Fastify | 比 raw `http.createServer` 更適合企業級，但保持輕量 |
| **WebSocket** | ws (same as OpenClaw) | 即時事件推送 |
| **Task Queue** | BullMQ (Redis-backed) | 分散式任務佇列，支援重試/延遲/優先度 |
| **ORM** | Drizzle ORM | TypeScript-first，零抽象成本 |
| **Validation** | Zod (same as OpenClaw) | 配置 + API Schema 驗證 |
| **Browser** | Playwright | 比 Puppeteer 更穩定的多瀏覽器支援 |
| **Docker SDK** | dockerode | 容器化沙盒管理 |

### 7.2 前端（Dashboard）

| 層級 | 技術 | 原因 |
|------|------|------|
| **Framework** | Next.js 15 (App Router) | SEO 平台 Dashboard 需 SSR + API Routes |
| **UI Library** | shadcn/ui + Tailwind CSS | 快速、美觀、可自訂 |
| **圖表** | Recharts + Plotly.js | 排名趨勢圖表 + 互動式數據視覺化 |
| **表格** | TanStack Table | 高效能大數據表格（關鍵字清單） |
| **即時更新** | SWR + WebSocket | API 快取 + 即時事件推送 |
| **State** | Zustand | 輕量、TypeScript-first |

### 7.3 數據庫

| 用途 | 技術 | 原因 |
|------|------|------|
| **主資料庫** | PostgreSQL 16 | JSONB + 全文搜尋 + 穩定性 |
| **向量搜尋** | pgvector (PG extension) | 語意搜尋，無需額外服務 |
| **任務佇列 + 快取** | Redis 7 | BullMQ 後端 + 快取 + Rate Limiter |
| **檔案儲存** | 本地 FS / S3 | 報告 PDF、爬蟲快照（MVP 不引入 MinIO） |
| **升級路徑** | 見 8.3 Scale-up Playbook | TimescaleDB / S3 / 讀寫分離 |

> **MVP 精簡原則**：僅使用 PostgreSQL + pgvector + Redis 三個系統。
> 排名歷史先用 PG 分區表儲存，升級步驟見 8.3 Scale-up Playbook。

### 7.4 基礎設施

| 用途 | 技術 | 原因 |
|------|------|------|
| **容器化** | Docker + Docker Compose | 開發環境 + 代理沙盒 |
| **部署** | Kubernetes (k8s) / Docker Swarm | 生產環境擴展 |
| **反向代理** | Caddy / Nginx | 自動 HTTPS + 負載均衡 |
| **監控** | Grafana + Prometheus | 系統健康 + 代理效能 |
| **日誌** | Loki (Grafana stack) | 集中式日誌管理 |
| **SERP 資料** | ValueSERP / Scale SERP API | API-First 策略，風險轉嫁給 SaaS 供應商 |

---

## 8. 資料庫與儲存設計

### 8.1 核心表結構

```sql
-- =============================================
-- 多租戶核心表（v2.1 新增）
-- =============================================

-- 租戶/組織
CREATE TABLE tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,                       -- 公司/組織名稱
  slug TEXT UNIQUE NOT NULL,                -- URL-safe 識別碼
  plan TEXT CHECK (plan IN ('starter', 'professional', 'enterprise')) DEFAULT 'starter',
  settings JSONB DEFAULT '{}',              -- 租戶級配置（LLM 預算上限、功能開關等）
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 使用者
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  name TEXT NOT NULL,
  password_hash TEXT NOT NULL,               -- bcrypt/argon2
  avatar_url TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 租戶成員關係（多對多 + 角色）
CREATE TABLE memberships (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role TEXT CHECK (role IN ('admin', 'manager', 'analyst')) NOT NULL DEFAULT 'analyst',
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(tenant_id, user_id)
);

-- =============================================
-- 業務表（所有表皆可透過 projects.tenant_id 推導租戶）
-- =============================================

-- 專案/客戶管理
CREATE TABLE projects (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,  -- 🔑 租戶歸屬
  name TEXT NOT NULL,
  domain TEXT NOT NULL,
  competitors JSONB DEFAULT '[]',          -- ["competitor1.com", ...]
  target_keywords JSONB DEFAULT '[]',
  settings JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_projects_tenant ON projects(tenant_id);

-- =============================================
-- PostgreSQL Row-Level Security (RLS)
-- 確保跨租戶查詢在 DB 層即被攔截
-- =============================================
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation_projects ON projects
  USING (tenant_id = current_setting('app.current_tenant_id')::uuid);

-- =============================================
-- 業務表 RLS 統一模板
-- 業務表（keywords, content, backlinks, page_audits, rank_history）
-- 僅有 project_id，需透過 JOIN projects 驗證 tenant_id
-- =============================================
-- 以 keywords 為例：
ALTER TABLE keywords ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation_keywords ON keywords
  USING (project_id IN (
    SELECT id FROM projects
    WHERE tenant_id = current_setting('app.current_tenant_id')::uuid
  ));

-- 同理套用至 content, backlinks, page_audits, rank_history：
-- ALTER TABLE <table> ENABLE ROW LEVEL SECURITY;
-- CREATE POLICY tenant_isolation_<table> ON <table>
--   USING (project_id IN (
--     SELECT id FROM projects
--     WHERE tenant_id = current_setting('app.current_tenant_id')::uuid
--   ));

-- =============================================
-- 🔒 Middleware 強制規格（工程強制項）
-- 每個 HTTP request / WebSocket 連線取得 DB connection 後，
-- 必須立即執行 SET，否則 RLS 將拒絕所有查詢。
-- =============================================
-- 應用層 Middleware 範例（Fastify onRequest hook）：
-- fastify.addHook('onRequest', async (req) => {
--   const tenantId = extractTenantId(req);  // 從 JWT / API Key 解析
--   const conn = await pool.connect();
--   await conn.query("SET app.current_tenant_id = $1", [tenantId]);
--   req.dbConn = conn;
-- });
--
-- ⚠️ 此為工程強制項：PR Review Checklist 須包含
--    「是否在取得 DB 連線後立即 SET app.current_tenant_id」

-- 關鍵字資料
CREATE TABLE keywords (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  keyword TEXT NOT NULL,
  search_volume INTEGER,
  difficulty REAL,
  cpc REAL,
  intent TEXT CHECK (intent IN ('informational', 'navigational', 'transactional', 'commercial')),
  cluster_id UUID,                          -- Topic Cluster 歸屬
  quick_win_score REAL,
  metadata JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(project_id, keyword)
);

-- 排名歷史（分區表，MVP 用 PG 原生分區）
CREATE TABLE rank_history (
  time TIMESTAMPTZ NOT NULL,
  project_id UUID REFERENCES projects(id),
  keyword_id UUID REFERENCES keywords(id),
  position INTEGER,
  url TEXT,
  serp_features JSONB DEFAULT '[]',         -- ["featured_snippet", "people_also_ask"]
  competitor_positions JSONB DEFAULT '{}',   -- {"competitor.com": 3, ...}
  PRIMARY KEY (time, keyword_id)
) PARTITION BY RANGE (time);
-- MVP: 按月手動分區
-- CREATE TABLE rank_history_2026_01 PARTITION OF rank_history FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');
-- Scale-up Playbook (Phase 2+): 見節 8.3

-- 頁面審計數據
CREATE TABLE page_audits (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  url TEXT NOT NULL,
  http_status INTEGER,
  title TEXT,
  title_length INTEGER,
  meta_description TEXT,
  meta_description_length INTEGER,
  h1_tags JSONB DEFAULT '[]',
  h2_tags JSONB DEFAULT '[]',
  canonical_url TEXT,
  has_schema BOOLEAN DEFAULT false,
  schema_types JSONB DEFAULT '[]',
  internal_links_count INTEGER,
  external_links_count INTEGER,
  broken_links JSONB DEFAULT '[]',
  page_speed_score REAL,
  lcp_ms REAL,
  fid_ms REAL,
  cls REAL,
  mobile_friendly BOOLEAN,
  crawled_at TIMESTAMPTZ DEFAULT now()
);

-- 內容管理
CREATE TABLE content (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  target_keyword_id UUID REFERENCES keywords(id),
  title TEXT NOT NULL,
  meta_title TEXT,
  meta_description TEXT,
  body_markdown TEXT,
  schema_markup JSONB,
  internal_links JSONB DEFAULT '[]',
  seo_score REAL,
  readability_score REAL,
  keyword_density REAL,
  word_count INTEGER,
  status TEXT CHECK (status IN ('draft', 'review', 'approved', 'published', 'needs-update')),
  published_url TEXT,
  published_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 反向連結數據
CREATE TABLE backlinks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  source_url TEXT NOT NULL,
  source_domain TEXT NOT NULL,
  target_url TEXT NOT NULL,
  anchor_text TEXT,
  domain_authority REAL,
  is_dofollow BOOLEAN DEFAULT true,
  first_seen_at TIMESTAMPTZ DEFAULT now(),
  last_seen_at TIMESTAMPTZ DEFAULT now(),
  status TEXT CHECK (status IN ('active', 'lost', 'new'))
);

-- 代理任務記錄
CREATE TABLE agent_tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  agent_id TEXT NOT NULL,
  project_id UUID REFERENCES projects(id),
  type TEXT NOT NULL,
  priority TEXT CHECK (priority IN ('critical', 'high', 'medium', 'low')),
  status TEXT CHECK (status IN ('queued', 'running', 'waiting-approval', 'completed', 'failed')),
  input JSONB,
  output JSONB,
  error TEXT,
  dependencies UUID[] DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now(),
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  retry_count INTEGER DEFAULT 0
);

-- 競爭對手快照
CREATE TABLE competitor_snapshots (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  domain TEXT NOT NULL,
  snapshot_type TEXT CHECK (snapshot_type IN ('content', 'backlink', 'serp')),
  data JSONB NOT NULL,
  diff_from_previous JSONB,
  captured_at TIMESTAMPTZ DEFAULT now()
);

-- 向量嵌入（用於語意搜尋）
CREATE TABLE embeddings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  source_type TEXT NOT NULL,             -- "keyword" | "content" | "audit"
  source_id UUID NOT NULL,
  embedding vector(1536),                -- OpenAI ada-002 / text-embedding-3-small
  created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX ON embeddings USING hnsw (embedding vector_cosine_ops);
```

### 8.2 索引策略

| 表 | 索引 | 用途 |
|---|---|---|
| `keywords` | `(project_id, keyword)` UNIQUE | 關鍵字去重 + 專案查詢 |
| `rank_history` | Partition Key `(time, keyword_id)` | 時間範圍查詢 + 按月分區 |
| `page_audits` | `(project_id, url)` | 頁面審計查詢 |
| `content` | `(project_id)` | 內容列表 |
| `embeddings` | HNSW `(embedding vector_cosine_ops)` | 向量近似搜尋 |
| `events_outbox` | `(dispatched, created_at) WHERE dispatched = false` | Outbox Dispatcher 輪詢（partial index） |
| `projects` | `(tenant_id)` | 多租戶隔離查詢 |

### 8.3 Scale-up Playbook（Phase 2+）

> **本節集中管理所有「MVP 之後」的儲存升級路徑，避免散落各章節造成矛盾。**

#### TimescaleDB 升級（排名歷史）

- **觸發條件**：`rank_history` 資料量超過 **1,000 萬行**，或月分區管理成本過高。
- **操作步驟**：

```sql
-- 1. 啟用 extension（同一 PG 實例，無需遷移）
CREATE EXTENSION IF NOT EXISTS timescaledb;

-- 2. 將現有分區表轉換為 hypertable
--    注意：需先合併所有子分區回母表，再執行 create_hypertable
SELECT create_hypertable('rank_history', 'time', migrate_data => true);

-- 3. 設定自動壓縮（30 天以上的資料自動壓縮）
ALTER TABLE rank_history SET (
  timescaledb.compress,
  timescaledb.compress_segmentby = 'keyword_id'
);
SELECT add_compression_policy('rank_history', INTERVAL '30 days');

-- 4. 設定自動保留（可選，保留 2 年資料）
-- SELECT add_retention_policy('rank_history', INTERVAL '2 years');
```

- **代碼影響**：零。應用層 SQL 查詢不變，TimescaleDB 向下相容標準 PG 語法。
- **監控指標**：`SELECT hypertable_size('rank_history');` 追蹤壓縮效果。

#### 檔案儲存升級（S3 / MinIO）

- **觸發條件**：報告 PDF + 爬蟲快照超過本地磁碟容量，或需要多節點共享存取。
- **操作**：引入 MinIO（自建）或切換至 AWS S3，統一使用 S3 SDK（`@aws-sdk/client-s3`）。

#### 讀寫分離 / 連線池

- **觸發條件**：DB 連線數超過 PG `max_connections` 限制，或讀取延遲影響 Dashboard 體驗。
- **操作**：引入 PgBouncer（連線池）+ PG 串流複寫（讀寫分離）。

---

## 9. API 設計與整合清單

### 9.1 內部 API（REST + WebSocket）

```yaml
# 專案管理
POST   /api/projects                      # 建立專案
GET    /api/projects/:id                  # 取得專案詳情
PATCH  /api/projects/:id                  # 更新專案設定

# 代理控制
POST   /api/agents/:agentId/run           # 手動觸發代理
GET    /api/agents/:agentId/status        # 代理狀態
GET    /api/agents/:agentId/logs          # 代理日誌
POST   /api/agents/:agentId/config        # 更新代理配置

# 關鍵字
GET    /api/projects/:id/keywords         # 關鍵字清單（分頁）
POST   /api/projects/:id/keywords/research # 觸發關鍵字研究
GET    /api/keywords/:id/ranks            # 排名歷史

# 內容
GET    /api/projects/:id/content          # 內容清單
POST   /api/projects/:id/content/generate # 觸發內容生成
PATCH  /api/content/:id/approve           # 審核文章
POST   /api/content/:id/publish           # 發布文章

# 審計
POST   /api/projects/:id/audit            # 觸發技術審計
GET    /api/projects/:id/audit/latest     # 最近一次審計結果

# 報告
GET    /api/projects/:id/reports          # 報告清單
GET    /api/reports/:id/download          # 下載 PDF

# 即時事件
WS     /ws/events                          # WebSocket 事件流
  → agent.task.started
  → agent.task.completed
  → agent.task.failed
  → rank.alert.drop
  → rank.alert.rise
  → approval.requested
  → report.ready
```

### 9.2 外部 API 整合清單

| API | 用途 | 代理使用 | 費用 |
|-----|------|---------|------|
| **Google Search Console** | 搜尋表現 + 排名數據 (L1) | report-generator, serp-tracker | 免費 |
| **Google Analytics 4** | 流量數據 (L1) | report-generator | 免費 |
| **Google PageSpeed Insights** | CWV 數據 | pagespeed-agent | 免費 (25K/day) |
| **Google NLP API** | 實體分析 | keyword-researcher | 按量計費 |
| **ValueSERP / Scale SERP** | **SERP 排名追蹤 (L2)** | **serp-tracker, competitor-monitor** | **$50-100/月** |
| **Ahrefs API** | 反向連結 + 關鍵字 + SERP (L2) | backlink-builder, keyword-researcher | $99+/月 |
| **SEMrush API** | 競爭分析 + SERP (L2) | competitor-monitor | $119+/月 |
| **WordPress REST API** | 內容發布 | content-writer | 免費 |
| **Shopify Admin API** | 電商內容管理 | content-writer | 免費 |
| **Slack Webhook** | 通知推送 | 所有代理 | 免費 |
| **Vercel AI SDK** | **統一 LLM Gateway（Anthropic/OpenAI/Google）** | **所有 Smart Agents** | **按量計費** |
| **Google Suggest** | 長尾關鍵字 | keyword-researcher | 無官方 API（極低頻爬取） |

> **LLM 模型路由說明**：透過 Vercel AI SDK 統一管理，不再分別維護 Anthropic SDK 和 OpenAI SDK。
> Smart Agents 使用 `smartModel`（Claude Opus 4 / Sonnet 4 / GPT-4o），Auto Tasks 的可選摘要使用 `cheapModel`（GPT-4o-mini / Claude Haiku）。

---

## 10. 安全架構

### 10.1 安全設計總覽

```
┌──────────────────────────────────────────────┐
│                  Security Layers              │
│                                               │
│  Layer 1: Network                             │
│  ├─ Tailscale / VPN（遠端存取）              │
│  ├─ Reverse Proxy + TLS（HTTPS only）        │
│  └─ Firewall rules（僅開放 80/443）          │
│                                               │
│  Layer 2: Authentication                      │
│  ├─ JWT Token（Dashboard 登入）              │
│  ├─ API Key（程式化存取）                    │
│  ├─ Timing-safe 比較（防時序攻擊）           │
│  └─ Rate Limiting（10 req/sec/IP）           │
│                                               │
│  Layer 3: Authorization (RBAC)                │
│  ├─ Admin: 完全控制                          │
│  ├─ Manager: 審核 + 配置                     │
│  ├─ Analyst: 只讀 Dashboard                  │
│  └─ Agent: 僅限分配的工具和 API              │
│                                               │
│  Layer 4: Agent Sandboxing                    │
│  ├─ Docker 容器隔離                          │
│  ├─ 網路白名單（per-agent）                  │
│  ├─ 檔案系統只讀（除工作目錄）               │
│  └─ CPU/Memory 限制                          │
│                                               │
│  Layer 5: Data Protection                     │
│  ├─ API Key 加密存儲（AES-256）              │
│  ├─ 配置文件敏感欄位掩碼                      │
│  ├─ Audit Log（所有操作記錄）                │
│  └─ 客戶數據隔離（多租戶）                    │
└──────────────────────────────────────────────┘
```

### 10.2 代理權限矩陣

| 代理 | 網路 | 檔案 | 外部 API | Docker | 審核需求 |
|------|------|------|---------|--------|---------|
| keyword-researcher | ✅ 限定域名 | 讀寫/workspace | Google, Ahrefs | ✅ | ❌ |
| content-writer | ❌ 無 | 讀寫/workspace | LLM only | ❌ | ✅ 發布前 |
| technical-auditor | ✅ 限定域名 | 讀寫/workspace | PSI, Lighthouse | ✅ | ❌ |
| backlink-builder | ✅ 限定域名 | 讀寫/workspace | Ahrefs | ✅ | ✅ Outreach 前 |
| competitor-monitor | ✅ 限定域名 | 讀寫/workspace | Ahrefs, SEMrush | ✅ | ❌ |
| report-generator | ✅ Google APIs | 讀寫/workspace | GA4, GSC | ❌ | ❌ |
| serp-tracker | ✅ Proxy Pool | 讀寫/workspace | ❌ | ✅ | ❌ |

---

## 11. Web Dashboard 設計

### 11.1 頁面結構

```
SEO Platform Dashboard
├── 🏠 Overview (總覽)
│   ├── 關鍵指標卡片（有機流量、排名數、已追蹤關鍵字、內容數量）
│   ├── 排名趨勢圖（30 天）
│   ├── 代理活動時間軸
│   └── 待審核項目清單
│
├── 🔍 Keywords (關鍵字中心)
│   ├── 關鍵字總表（搜尋量/難度/排名/意圖/群組）
│   ├── Quick Win 機會面板
│   ├── Topic Cluster 可視化
│   └── 關鍵字研究觸發器
│
├── 📊 Rank Tracker (排名追蹤)
│   ├── 每日排名變化表
│   ├── 排名分布圖（Top 3 / Top 10 / Top 20 / 20+）
│   ├── SERP Feature 追蹤
│   └── 警報設定
│
├── ✍️ Content Hub (內容中心)
│   ├── 內容行事曆（甘特圖）
│   ├── 文章編輯器（Markdown + SEO 評分側邊欄）
│   ├── 審核佇列
│   └── 已發布內容效能追蹤
│
├── 🔧 Technical Audit (技術審計)
│   ├── 網站健康分數總覽
│   ├── 問題清單（Critical / Warning / Info）
│   ├── Core Web Vitals 趨勢
│   └── 爬取覆蓋率地圖
│
├── 🔗 Backlinks (反向連結)
│   ├── Backlink Profile 總覽（DA 分布圖）
│   ├── 新增/丟失連結追蹤
│   ├── Outreach 管理面板
│   └── 競品 Backlink Gap 分析
│
├── 🏪 Local SEO
│   ├── GMB Profile 管理
│   ├── Citation 清單
│   └── Review 監控 + 回應佇列
│
├── 📈 Reports (報告中心)
│   ├── 自動報告清單（PDF 下載）
│   ├── 自訂報告建構器
│   └── 白標報告設定（客戶品牌客製化）
│
├── 🤖 Agents (代理管理)
│   ├── 12 個代理的狀態面板
│   ├── 代理活動日誌
│   ├── 排程管理（Cron 設定）
│   └── 代理配置編輯器
│
├── ⚙️ Settings (系統設定)
│   ├── 專案管理
│   ├── API Key 管理
│   ├── 通知設定
│   ├── 用戶/角色管理 (RBAC)
│   └── 備份/匯出
│
└── 🛡️ Security (安全審計)
    ├── 安全審計報告
    ├── 操作日誌
    └── API 使用量統計
```

---

## 12. 開發階段與里程碑

### Phase 0: 基礎建設（第 1-4 週）

```yaml
目標: 開發環境 + 核心框架 + 風險驗證

任務:
  基礎構建（第 1-2 週）:
  - [ ] OpenClaw 本地對照基準建立（工程參考：`C:\\Users\\User\\Desktop\\openclaw`）
  - [ ] 初始化 Monorepo（pnpm workspace）
  - [ ] 配置 TypeScript + ESLint + Prettier
  - [ ] Docker Compose（PostgreSQL + pgvector + Redis）  # MVP 精簡：僅 3 系統
  - [ ] CI/CD Pipeline（GitHub Actions）
  - [ ] 基礎 Fastify Server + WebSocket
  - [ ] Zod Config Schema（核心配置結構）
  - [ ] Drizzle ORM + 資料庫 Migration（含 tenants/users/memberships + RLS）
  - [ ] Vercel AI SDK 初始化（統一 LLM Gateway）

  風險驗證（第 3-4 週）:
  - [ ] **AI SDK Spike 驗證**：以 content-writer 為標的，驗證多輪 tool use + streaming + retry + token counting 在 Node.js 後端的完整性。若 Vercel AI SDK 不足以支撐，備案為 LiteLLM 或直接用 provider SDK + 自建路由層。
  - [ ] Outbox Dispatcher 雛型（events_outbox 表 + 輪詢投遞 + 重試機制）
  - [ ] RLS 整合測試：驗證 Middleware SET app.current_tenant_id 與多表 RLS Policy 聯動
  - [ ] Spike 結果評審 + Go/No-Go 決策

產出: 可運行的骨架專案 + 資料庫 Schema + AI SDK Spike 報告
```

> **為什麼 4 週而非 2 週？** v2.1 新增了 AI SDK Spike、Outbox Dispatcher、多租戶 RLS 等風險驗證項目，團隊討論後認為 2 週無法完成。擴為 4 週后總時程從 24 週調整為 **28 週（7 個月）**。

### Phase 1: 核心引擎（第 5-8 週）

```yaml
目標: Orchestrator + Agent Runtime + 基礎代理

任務:
  - [ ] Orchestrator 核心（Task Queue + DAG 解析器）
  - [ ] Agent Runtime（隔離 Workspace + 記憶體 + 沙盒）
  - [ ] Event Bus（Agent 間通訊）
  - [ ] Plugin System（工具註冊 + 發現）
  - [ ] 基礎 Browser Engine（Playwright wrapper）
  - [ ] Memory Store（PostgreSQL + pgvector）
  - [ ] Cron Scheduler（BullMQ）
  - [ ] Notification Hub（Slack + Dashboard push）
  
  引擎驗證（2 個代理 MVP）:
  - [ ] keyword-researcher Agent
  - [ ] serp-tracker Agent

產出: 可以執行關鍵字研究 + 排名追蹤的核心引擎
```

### Phase 2: 完整代理池（第 9-14 週）

```yaml
目標: 6 Smart Agents + 6 Auto Tasks 全部上線

任務:
  批次 A（第 9-10 週）:
  - [ ] content-writer Agent（🧠 Smart）
  - [ ] technical-auditor Agent（⚙️ Auto Task）
  - [ ] competitor-monitor Agent（🧠 Smart）
  
  批次 B（第 11-12 週）:
  - [ ] backlink-builder Agent（🧠 Smart）
  - [ ] report-generator Agent（🧠 Smart）
  - [ ] schema-agent Agent（⚙️ Auto Task）
  
  批次 C（第 13-14 週）:
  - [ ] internal-linker Agent（⚙️ Auto Task）
  - [ ] pagespeed-agent Agent（⚙️ Auto Task）
  - [ ] local-seo Agent（⚙️ Auto Task）
  - [ ] content-refresher Agent（🧠 Smart）

  代理協作:
  - [ ] 完整的 5 階段工作流（研究→規劃→生產→發布→監控）
  - [ ] Human-in-the-Loop 審核機制
  - [ ] 代理間任務委派（Subagent pattern）

產出: 全部 12 個代理可協同工作
```

### Phase 3: Dashboard（第 15-20 週）

```yaml
目標: 完整的 Web Dashboard

任務:
  批次 A（第 15-16 週）:
  - [ ] Next.js 專案初始化 + Auth（JWT）
  - [ ] Overview Dashboard
  - [ ] Agent Status Panel
  
  批次 B（第 17-18 週）:
  - [ ] Keyword Explorer
  - [ ] Rank Tracker（圖表）
  - [ ] Content Hub（編輯器 + 審核佇列）
  
  批次 C（第 19-20 週）:
  - [ ] Technical Audit Viewer
  - [ ] Backlink Manager
  - [ ] Report Center（PDF 下載）
  - [ ] Settings / RBAC

產出: 完整可用的 Web 管理面板
```

### Phase 4: 企業功能（第 21-28 週）

```yaml
目標: 生產就緒 + 企業功能

任務:
  多租戶應用層（Phase 0 已完成 DB 層 RLS 隔離，此階段補齊應用層）:
  - [ ] 租戶管理 UI（建立 / 停用 / 刪除租戶）
  - [ ] 租戶自助 Onboarding 流程
  - [ ] 計費配額限制（每租戶關鍵字上限 / API 呼叫配額 / 爬取配額）
  - [ ] 租戶專屬 Dashboard 視圖

  企業功能:
  - [ ] 白標報告（客戶品牌客製化）
  - [ ] API 文件（OpenAPI / Swagger）
  - [ ] 第三方 Webhook 整合
  - [ ] 進階 RBAC（團隊成員權限管理）
  - [ ] 備份與恢復

  品質保證:
  - [ ] 效能優化與負載測試
  - [ ] 安全審計與滲透測試
  - [ ] 部署文件 + Docker/K8s 配置
  - [ ] 使用者文件 + 影片教學

產出: 可對外交付的企業級產品 v1.0
```

---

## 13. 團隊組成與分工

### 13.1 核心團隊（8 人）

```
├── 🏗️ 技術主管 / 架構師    ×1
│   └─ 整體架構設計、技術決策、Code Review
│
├── 🔧 後端工程師            ×3
│   ├─ #1: Orchestrator + Event Bus + Plugin System
│   ├─ #2: Agent Runtime + Sandbox + Browser Engine
│   └─ #3: API + 整合 + Memory Store
│
├── 🎨 前端工程師            ×2
│   ├─ #1: Dashboard 核心 + 圖表 + 即時更新
│   └─ #2: Content Editor + Audit Viewer + Settings
│
├── 🧪 QA 工程師             ×1
│   └─ 自動化測試 + 整合測試 + 安全測試
│
└── 📊 SEO 領域專家（顧問）  ×1
    └─ Agent Prompt 設計 + 工作流程驗證 + 品質標準定義
```

### 13.2 各角色工作分配

| 角色 | Phase 0-1 | Phase 2 | Phase 3 | Phase 4 |
|------|-----------|---------|---------|---------|
| 架構師 | 系統設計 + 核心框架 | 代理協作機制 | API 設計審查 | 效能調優 + 安全審計 |
| 後端 #1 | BullMQ Flow Orchestrator | 代理間委派 | Webhook 整合 | 多租戶 |
| 後端 #2 | Agent Runtime + SERP API 層 | 6 個代理實作 | 沙盒強化 | K8s 部署 |
| 後端 #3 | API + DB + Vercel AI SDK | 6 個代理實作 | API 文件 | 備份恢復 |
| 前端 #1 | — | — | Dashboard 核心 | 效能優化 |
| 前端 #2 | — | — | 內容編輯器 | 白標報告 |
| QA | 測試框架搭建 | 代理功能測試 | E2E 測試 | 安全 + 負載測試 |
| SEO 專家 | 需求釐清 | Prompt 撰寫 | UX 驗證 | 使用者文件 |

---

## 14. 成本估算

### 14.1 開發成本（7 個月 / 28 週）

> **v2.2 更新**：Phase 0 從 2 週擴為 4 週（含 AI SDK Spike + Outbox + RLS 驗證），總時程從 24 週調整為 28 週（7 個月）。人力月數相應調整。

| 項目 | 月費 (NTD) | 人數 | 月數 | 小計 (NTD) |
|------|-----------|------|------|-----------|
| 技術主管 | 120,000 | 1 | 7 | 840,000 |
| 後端工程師 | 90,000 | 3 | 7 | 1,890,000 |
| 前端工程師 | 85,000 | 2 | 4 | 680,000 |
| QA 工程師 | 75,000 | 1 | 6 | 450,000 |
| SEO 顧問（兼職） | 40,000 | 1 | 7 | 280,000 |
| **人力小計** | | | | **4,140,000** |

| 項目 | 月費 (NTD) | 月數 | 小計 (NTD) |
|------|-----------|------|-----------|
| 雲端伺服器（開發環境） | 15,000 | 7 | 105,000 |
| API 費用（LLM + SEO 工具 + **SERP SaaS API**） | 25,000 | 7 | 175,000 |
| SaaS 工具（GitHub, Figma 等） | 8,000 | 7 | 56,000 |
| **基礎設施小計** | | | **336,000** |

| | **NTD** | **USD (approx.)** |
|---|---------|-------------------|
| **總開發成本** | **4,476,000** | **~$138,000** |

> **v2.2 vs v2.0 差異**：
> - 人力成本增加 NTD 505,000（+1 月 × 技術主管/後端/QA/SEO 顧問）。
> - 基礎設施增加 NTD 48,000（+1 月 × 雲端/API/SaaS）。
> - 總開發成本增加 NTD 553,000（+14%），換取 Phase 0 風險充分驗證，避免 Phase 1 以後返工。

### 14.2 上線後營運成本（月）

| 項目 | 月費 (NTD) |
|------|-----------|
| 雲端主機（生產環境） | 25,000 - 50,000 |
| LLM API 費用（透過 Vercel AI SDK 統一計量） | 15,000 - 40,000 |
| SEO 工具 API + **SERP SaaS API** | 12,000 - 25,000 |
| 監控 + 日誌 | 3,000 |
| **月營運成本** | **55,000 - 118,000** |

---

## 15. 風險矩陣與應對策略

| # | 風險 | 機率 | 影響 | 應對策略 |
|---|------|------|------|---------|
| 1 | **SERP 資料取得中斷** | 中 | 高 | **API-First 三級策略**：L1 GSC → L2 SERP SaaS API → L3 自建爬取備援；風險轉嫁給 SaaS 供應商 |
| 2 | **LLM 幻覺（生成錯誤數據）** | 中 | 高 | 結構化驗證 + 人工審核閘門 + 事實查核代理 |
| 3 | **API Rate Limit** | 高 | 中 | 多 Key 輪替 + BullMQ 智慧排隊 + 本地快取 |
| 4 | **Prompt Injection** | 中 | 高 | 輸入消毒 + 沙盒隔離 + 最小權限原則 |
| 5 | **SEO 演算法變更** | 中 | 中 | 模組化設計 + 快速迭代 + SEO 專家持續顧問 |
| 6 | **開發延期** | 中 | 中 | 敏捷開發 + 2 週 Sprint + MVP 優先 |
| 7 | **數據隱私合規 (GDPR)** | 低 | 高 | 資料本地化選項 + 加密存儲 + 審計日誌 |
| 8 | **團隊流失** | 中 | 高 | 完整文件 + Code Review + 知識共享 |

---

## 16. 商業化路線圖

### 16.1 定價模型

> **⚠️ 定價待驗證**：以下為初版定價假設，需在 Closed Beta 階段進行價值驗證。
> - **Starter NTD 2,990 / 50 關鍵字**：需確認目標用戶（個人 SEO 專員）是否願意付此價位，或需調整為 freemium 模型。
> - **目標市場跨度風險**：同時服務個人→工作室→代理商→私有部署，MVP 應聚焦 1-2 個客群，建議優先瞄準 **Professional（SEO 工作室）** 和 **Enterprise（行銷代理商）**。

```yaml
Starter（個人 SEO 專員）:
  價格: NTD 2,990 / 月
  包含:
    - 1 個專案
    - 5 個核心代理（研究/內容/審計/排名/報告）
    - 50 個追蹤關鍵字
    - 每月 500 篇頁面爬取
    - Email 報告

Professional（SEO 工作室）:
  價格: NTD 9,900 / 月
  包含:
    - 10 個專案
    - 全部 12 個代理
    - 500 個追蹤關鍵字
    - 每月 10,000 篇頁面爬取
    - PDF 白標報告
    - Slack 整合
    - 3 名團隊成員

Enterprise（行銷代理商）:
  價格: NTD 29,900 / 月
  包含:
    - 無限專案
    - 全部代理 + 自訂代理
    - 5,000 個追蹤關鍵字
    - 每月 100,000 篇頁面爬取
    - 完整 API 存取
    - 多租戶白標
    - 無限團隊成員
    - 優先支援

Self-Hosted（私有部署）:
  價格: NTD 500,000（一次性授權）+ NTD 100,000/年 維護
  包含:
    - 完整原始碼授權
    - 部署協助
    - 1 年更新
```

### 16.2 Go-to-Market 策略

```
Month 1-2: Closed Beta
├── 邀請 10 位 SEO 專員內測
├── 收集回饋 + 修復問題
└── 建立案例研究

Month 3-4: Public Beta
├── ProductHunt 發布
├── SEO 社群推廣（台灣 SEO 社團、PTT）
├── 內容行銷（Medium / Blog）
└── YouTube 教學影片

Month 5-6: 正式上線
├── 付費方案啟動
├── 合作夥伴計畫（SEO 工具整合商）
└── 企業客戶開發
```

---

## 附錄 A：專案目錄結構

```
seo-ai-platform/
├── apps/
│   ├── api/                        # Fastify API Server
│   │   ├── src/
│   │   │   ├── server.ts           # 入口（參考 OpenClaw server.impl.ts）
│   │   │   ├── routes/             # API Routes
│   │   │   ├── middleware/         # Auth, Rate Limit, RBAC
│   │   │   └── websocket/         # WS Handler
│   │   └── package.json
│   │
│   └── web/                        # Next.js Dashboard
│       ├── src/
│       │   ├── app/                # App Router pages
│       │   ├── components/         # UI components
│       │   └── lib/                # API client, hooks
│       └── package.json
│
├── packages/
│   ├── core/                       # 共享核心邏輯
│   │   ├── src/
│   │   │   ├── orchestrator/       # BullMQ Flow 工作流引擎
│   │   │   │   ├── flow-producer.ts
│   │   │   │   ├── workers.ts
│   │   │   │   └── dead-letter.ts
│   │   │   ├── agent-runtime/      # 代理執行環境（Smart / Auto Task）
│   │   │   │   ├── runtime.ts
│   │   │   │   ├── sandbox.ts
│   │   │   │   └── session.ts
│   │   │   ├── event-bus/          # 通知/日誌層
│   │   │   │   └── events.ts
│   │   │   ├── serp-data/          # SERP 資料層（API-First）
│   │   │   │   ├── gsc-client.ts
│   │   │   │   ├── serp-saas.ts
│   │   │   │   └── site-crawler.ts
│   │   │   ├── llm-gateway/        # Vercel AI SDK 統一抽象
│   │   │   │   ├── provider.ts
│   │   │   │   └── model-router.ts
│   │   │   ├── memory/             # PG + pgvector
│   │   │   │   ├── store.ts
│   │   │   │   └── search.ts
│   │   │   ├── scheduler/          # Cron 排程
│   │   │   │   └── cron.ts
│   │   │   ├── notifications/      # 通知中心
│   │   │   │   └── hub.ts
│   │   │   ├── security/           # 安全模組
│   │   │   │   ├── auth.ts
│   │   │   │   ├── rbac.ts
│   │   │   │   └── audit.ts
│   │   │   └── config/             # 配置驗證
│   │   │       ├── schema.ts
│   │   │       └── validation.ts
│   │   └── package.json
│   │
│   ├── agents/                     # 6 Smart Agents + 6 Auto Tasks
│   │   ├── keyword-researcher/
│   │   │   ├── agent.ts
│   │   │   ├── tools/
│   │   │   │   ├── google-suggest.ts
│   │   │   │   ├── ahrefs-keywords.ts
│   │   │   │   └── intent-classifier.ts
│   │   │   ├── prompts/
│   │   │   │   └── system.md
│   │   │   └── package.json
│   │   ├── content-writer/
│   │   ├── technical-auditor/
│   │   ├── backlink-builder/
│   │   ├── competitor-monitor/
│   │   ├── local-seo/
│   │   ├── report-generator/
│   │   ├── schema-agent/
│   │   ├── internal-linker/
│   │   ├── pagespeed-agent/
│   │   ├── serp-tracker/
│   │   └── content-refresher/
│   │
│   ├── integrations/               # 第三方 API 整合
│   │   ├── google/                 # GA4, GSC, PSI
│   │   ├── ahrefs/
│   │   ├── semrush/
│   │   ├── serp-saas/              # ValueSERP, Scale SERP
│   │   ├── wordpress/
│   │   └── shopify/
│   │
│   └── shared/                     # 共享工具
│       ├── types/
│       ├── utils/
│       └── test-helpers/
│
├── docker/
│   ├── docker-compose.yml          # 開發環境
│   ├── docker-compose.prod.yml     # 生產環境
│   ├── Dockerfile.api
│   ├── Dockerfile.web
│   └── sandbox/
│       ├── Dockerfile.sandbox
│       └── Dockerfile.browser
│
├── docs/
│   ├── architecture.md
│   ├── api-reference.md
│   ├── deployment.md
│   └── agent-development.md
│
├── scripts/
│   ├── seed.ts                     # 測試數據填充
│   ├── migrate.ts                  # 資料庫遷移
│   └── benchmark.ts               # 效能測試
│
├── drizzle/
│   └── migrations/                 # DB Migrations
│
├── pnpm-workspace.yaml
├── tsconfig.json
├── package.json
└── README.md
```

---

## 附錄 B：MVP 快速驗證路線（4 週版本）

如果希望快速驗證概念，可以先實作最核心的 3 個代理：

```
Week 1: 基礎框架 + keyword-researcher
Week 2: serp-tracker + 排名追蹤 DB
Week 3: report-generator + 簡易 Dashboard
Week 4: 整合測試 + 部署 Demo

MVP 功能:
✅ 關鍵字研究（種子關鍵字 → 擴展清單）
✅ 每日排名追蹤（Top 50 關鍵字）
✅ 自動週報生成（PDF）
✅ Slack 通知（排名變動警報）
✅ 簡易 Web Dashboard（排名趨勢圖）
```

---

*本文件由系統自動生成，基於 OpenClaw v2026.2.13 架構分析*
*最後更新：2026-02-15*
