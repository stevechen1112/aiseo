# Agent Field UI — 皮克敏風格多代理視覺化設計計畫

> **發想來源**：皮克敏（Pikmin）遊戲機制與 AISEO Multi-Agent 系統的完美對應
> **建立日期**：2026-02-19
> **最後更新**：2026-02-19（v1.1：整合 SVG + Rive 路線與新版 Todo）
> **狀態**：✅ 已完成（24/24）

---

## 📌 設計理念

皮克敏的遊戲機制與 Multi-Agent 系統幾乎是一對一對應：

| 皮克敏元素 | AISEO 系統對應 | UI 呈現方式 |
|---|---|---|
| 隊長 (Olimar) | Orchestrator Engine | 中央指揮游標 / 基地圖示 |
| 不同顏色皮克敏 | 12 種 Agent 類型 | 各色幾何機器人角色 |
| 拔出皮克敏 | 啟動 Agent | 從地面冒出動畫 |
| 投擲皮克敏 | 派發任務 (BullMQ Job) | 拋物線飛行動畫 |
| 皮克敏搬運 | Agent 處理資料 | 小人搬箱子動畫 |
| 花苞→花朵 | Agent 進度成長 | 進度條長花特效 |
| 夜晚回巢 | Worker idle 狀態 | 小人跑回基地 |
| 被敵人吃掉 | Job failed | 小人消失特效 + 復活btn |

---

## ⚖️ 版權策略

**不使用任何任天堂資產，完全原創：**

- ✅ Phase A 使用 **純手寫 SVG**（完全原創，0 版權風險）
- ✅ Phase B 可整合 **Rive Community 免費資產**（僅使用可商用授權素材）
- ✅ 可選備援：**Kenney.nl Robot Pack**（CC0 授權，完全免費商用）
- ✅ 幾何風格小機器人，自訂顏色對應 Agent 類型
- ✅ 動畫使用 **Framer Motion + Rive**
- ✅ 場景使用純 SVG + CSS（自繪）
- ✅ 功能名稱：「**AISEO Agent Field**」（非「皮克敏」）
- ❌ 不使用 Pikmin 名稱、造型、音效

---

## 🎨 Agent 角色設計規範

### 顏色對應表

| Agent ID | 類型 | 顏色 | Hex | 角色個性 |
|---|---|---|---|---|
| `keyword-researcher` | Smart Agent 🧠 | 藍色 | `#3B82F6` | 善於挖掘、探索 |
| `content-writer` | Smart Agent 🧠 | 紅色 | `#EF4444` | 創作力強、戰鬥型 |
| `competitor-monitor` | Smart Agent 🧠 | 紫色 | `#8B5CF6` | 力量最大、情報型 |
| `backlink-builder` | Smart Agent 🧠 | 橙色 | `#F97316` | 堅硬、建築型 |
| `report-generator` | Smart Agent 🧠 | 粉色 | `#EC4899` | 會飛、速度型 |
| `content-refresher` | Smart Agent 🧠 | 青色 | `#06B6D4` | 清爽、更新型 |
| `serp-tracker` | Auto Task ⚙️ | 黃色 | `#EAB308` | 觀察型 |
| `technical-auditor` | Auto Task ⚙️ | 綠色 | `#22C55E` | 分析型 |
| `schema-agent` | Auto Task ⚙️ | 靛色 | `#6366F1` | 結構型 |
| `internal-linker` | Auto Task ⚙️ | 檸檬 | `#84CC16` | 連結型 |
| `pagespeed-agent` | Auto Task ⚙️ | 天藍 | `#0EA5E9` | 速度型 |
| `local-seo` | Auto Task ⚙️ | 棕色 | `#A78BFA` | 本地型 |

### 角色視覺區分

```
Smart Agents (🧠) → 機器人頭上有「天線花朵」裝飾 (進化完全)
Auto Tasks  (⚙️) → 機器人頭上有「⚙️齒輪」裝飾 (規則驅動)
```

### 狀態動畫對應

| Agent 狀態 | 動畫 | CSS/Framer |
|---|---|---|
| `idle` | 原地左右踱步 / 微晃 | `animate-pulse` + 輕微左右移 |
| `running` | 快速跑向任務方向 | `animate-bounce` + x 軸平移 |
| `waiting` | 坐下等待 | 靜止 + `opacity-75` |
| `completed` | 舉手歡呼後走回基地 | 縮放 sparkle 特效 |
| `failed` | 倒地灰階 | `grayscale filter` + 淡出 |
| `paused` | 停在半路打哈欠 | 靜止 + 慢速 pulse |

---

## 🗺️ 場景佈局設計

```
┌─────────────────────────────────────────────────────────────┐
│  🌿  AISEO Agent Field              📅 Day 42  🕐 14:32    │
│  ─────────────────────────────────────────────────────────  │
│                                                              │
│   ┌──────────┐                                              │
│   │  🏠 基地  │  ← Orchestrator / Onion Base               │
│   │  [===]   │     (租戶升級時動畫變大)                    │
│   └──────────┘                                              │
│        │                                                     │
│   🤖🤖🤖🤖  ← idle agents 在基地附近待機                   │
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │ 📦 Keyword  │  │ 📦 Content  │  │ 📦 Audit    │        │
│  │  Research  │  │  Writing    │  │   Task      │        │
│  │ 🔵🔵🔵→→  │  │  🔴🔴→→   │  │   🟡→→      │        │
│  │ 進行中 3/5 │  │ 進行中 2/3  │  │ 進行中 1/2  │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                              │
│  ⚠️ Failed Zone:   💀 backlink job #4821 [復活]            │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🔵×3  🔴×2  🟡×1  💜×0  🩷×1  🪨×0                │   │
│  │ 出動中: 7/12    基地待命: 5    今日失敗: 2 💀       │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 檔案結構規劃

```
apps/web/src/
├── app/dashboard/
│   ├── agents/
│   │   ├── page.tsx                    ← 現有 (保留，加 View Toggle)
│   │   └── field/
│   │       └── page.tsx                ← 新增：Field 視圖主頁
│   └── ...
├── components/
│   └── agent-field/                    ← 新增目錄
│       ├── AgentFieldScene.tsx         ← SVG 草地場景容器
│       ├── AgentBot.tsx                ← 單一機器人角色元件
│       ├── AgentBotAnimated.tsx        ← 帶 Framer Motion 動畫版本
│       ├── TaskBox.tsx                 ← 任務箱圖示元件
│       ├── BaseHQ.tsx                  ← 基地/洋蔥艙元件
│       ├── AgentCounterHUD.tsx         ← 左上角計數器 HUD
│       ├── ThrowAnimation.tsx          ← 拋物線投擲動畫
│       └── FailedZone.tsx              ← 失敗區域元件
└── lib/
    └── agent-field-utils.ts            ← 位置計算、狀態映射工具函式
```

---

## 🛠️ 技術選型

| 需求 | 選擇 | 理由 |
|---|---|---|
| 角色動畫 | **Phase A: CSS/Framer、Phase B: Rive** | 先快上線，再升級動態品質 |
| 角色素材 | **純 SVG（自繪）+ Rive Free + Kenney CC0(備援)** | 0 版權風險，並保留動態升級路徑 |
| 場景渲染 | **SVG（非 Canvas/WebGL）** | 與 React/Next.js SSR 相容 |
| 狀態資料來源 | **現有 `/ws/events` WebSocket** | 後端完全不需改動 |
| 資料 Hooks | **現有 `useAgentActivities` + `useSchedules`** | 直接復用 |
| 樣式 | **Tailwind CSS** | 已在專案中 |

**不引入：**
- ❌ Pixi.js（WebGL，與 SSR 衝突，學習成本高）
- ❌ Phaser.js（完整遊戲引擎，過重）
- ❌ Matter.js（物理引擎，不需要）
- ❌ 任天堂任何圖像/音效/IP 相關資產

---

## 📋 任務計畫清單

### Phase A — 概念驗證（目標：2天）
> 目標：最小改動，立刻看到效果

- [x] **A-1** 在現有 `agents/page.tsx` 頂部加入「Field View / List View」切換按鈕
- [x] **A-2** 建立 `components/agent-field/AgentBot.tsx` — 純 SVG 幾何機器人（無動畫）
- [x] **A-3** 建立顏色映射常數 `AGENT_COLORS` 與 `AGENT_ICONS`
- [x] **A-4** 加入 CSS 動畫類：`animate-bounce`(running), `animate-pulse`(idle), `grayscale`(failed)
- [x] **A-5** 新增 `/dashboard/agents/field/page.tsx` 頁面，顯示靜態草地場景
- [x] **A-6** 在 field view 渲染所有 agent 的 `AgentBot` 元件，顏色/狀態對應正確

**驗收標準：**
- 可在 List View / Field View 之間切換
- 12 個 agent 各有對應顏色的幾何機器人
- 狀態改變時 CSS 動畫即時切換

---

### Phase B — 動態動畫（目標：1週）
> 目標：真正的皮克敏「生命感」

- [x] **B-0** 安裝 `@rive-app/react-canvas`
- [x] **B-1** 下載/製作可商用授權的 Rive 機器人動畫（含授權來源註記）
- [x] **B-2** 建立 `AgentBotRive.tsx` — Rive 狀態機版本（idle/running/failed/completed）
- [x] **B-3** 建立 `BaseHQ.tsx` — 基地元件（SVG 小屋），idle agents 聚集周圍
- [x] **B-4** 建立 `TaskBox.tsx` — 任務箱圖示，顯示任務名稱和進行中的 agent 數量
- [x] **B-5** 建立 `AgentCounterHUD.tsx` — 左上角計數器
  - 各類型 running job 數量
  - 出動中 / 基地待命 / 今日失敗 統計
- [x] **B-6** WebSocket 事件即時驅動動畫狀態切換（監聽現有 `/ws/events`）
- [x] **B-7** `agent-field-utils.ts` — 計算各 agent 在 SVG 場景中的位置邏輯
  - idle agents → 基地附近隨機散佈
  - running agents → 移向對應任務箱
  - failed agents → 移至右下角 Failed Zone

**驗收標準：**
- 角色狀態改變時動畫流暢切換（無跳閃）
- WebSocket 事件觸發即時反映在場景中
- HUD 數字與實際 job 數量一致

---

### Phase C — 互動功能（目標：選做，2週）
> 目標：可操作的「投擲派任務」體驗

- [x] **C-1** 建立 `ThrowAnimation.tsx` — 點擊「Run Agent」時的拋物線動畫
  - 用 Framer Motion `useMotionValue` 模擬拋物線軌跡
  - 角色從基地拋出，落地後進入 running 動畫
- [x] **C-2** 點擊 `TaskBox` 選中任務 → 游標變成「瞄準」樣式
- [x] **C-3** 點擊已選中任務 + 點擊閒置 agent → 觸發派任務 API（對應現有 `runSchedule`）
- [x] **C-4** **日落計時器**：顯示下一個 scheduled job 的倒計時，時間到時場景轉暗
- [x] **C-5** **配額危機警示**：Quota > 80% 時，場景出現紅色警示光暈 + 震動效果
- [x] **C-6** **彩蛋 — 成就訊息**：job 完成時隨機顯示皮克敏風格成就文字
  - 「今天的關鍵字研究員挖出了 500 個關鍵字！」
  - 「內容機器人完成了它的使命！」

**驗收標準：**
- 拋物線動畫視覺上直覺自然
- 投擲操作實際觸發 API，job 進入隊列

---

### Phase D — 打磨細節（目標：選做）
> 目標：生產品質的視覺體驗

- [x] **D-1** SVG 草地場景美術優化（加入樹木、石頭裝飾物）
- [x] **D-2** 租戶升級方案時，基地動畫變大（Stripe Webhook → WebSocket 事件驅動）
- [x] **D-3** 行動裝置響應式調整（SVG viewBox 自適應）
- [x] **D-4** Dark mode 場景（夜晚版草地背景，與系統 dark mode 同步）

---

## 🔗 資料流說明

```
後端 (完全不需改動)
    │
    ├── GET /api/schedules         →  useSchedules()       → 顯示哪些 agent 已啟用
    ├── GET /api/agents/activities →  useAgentActivities() → 顯示最近 job 記錄
    └── WS  /ws/events             →  useWebSocket()       → 即時狀態推送
                │
                ▼
前端 Agent Field UI
    │
    ├── AgentFieldScene.tsx   ← 訂閱 WebSocket，分發狀態給各子元件
    ├── AgentBotAnimated.tsx  ← 接收狀態 prop，播放對應動畫
    ├── TaskBox.tsx           ← 接收 job 資料，顯示進行中數量
    ├── AgentCounterHUD.tsx   ← 接收彙總統計
    └── BaseHQ.tsx            ← 訂閱租戶 plan，控制大小
```

---

## 📐 核心元件 API 設計

### `AgentBot.tsx`
```tsx
interface AgentBotProps {
  agentId: AgentId
  status: 'idle' | 'running' | 'completed' | 'failed' | 'paused' | 'waiting'
  size?: 'sm' | 'md' | 'lg'
  animated?: boolean
  showLabel?: boolean
}
```

### `AgentFieldScene.tsx`
```tsx
interface AgentFieldSceneProps {
  projectId: string
  width?: number   // SVG viewBox 寬度，預設 1200
  height?: number  // SVG viewBox 高度，預設 600
}
```

### `AgentCounterHUD.tsx`
```tsx
interface AgentCounterHUDProps {
  agentCounts: Record<AgentId, number>  // 各類型 running 數量
  totalDeployed: number
  totalIdle: number
  totalFailed: number  // 今日失敗數
}
```

---

## 🎯 里程碑時間表

| 里程碑 | 預計完成 | 狀態 |
|---|---|---|
| Phase A 完成 — 可切換 Field View | 已完成 | ✅ 完成 |
| Phase B 完成 — 動畫版本上線 | 已完成 | ✅ 完成 |
| Phase C 完成 — 投擲互動 | 已完成 | ✅ 完成 |
| Phase D 完成 — 生產品質 | 已完成 | ✅ 完成 |

---

## 📊 進度追蹤

**整體進度：** `24 / 24 任務完成`

```
Phase A: [x][x][x][x][x][x]               6/6
Phase B: [x][x][x][x][x][x][x][x]         8/8
Phase C: [x][x][x][x][x][x]               6/6
Phase D: [x][x][x][x]                     4/4 (選做)
```

---

## 📝 變更記錄

| 日期 | 版本 | 說明 |
|---|---|---|
| 2026-02-19 | v1.0 | 初版計畫建立 |
| 2026-02-19 | v1.1 | 整合 SVG + Rive 路線，任務同步為 24 項 Todo |
| 2026-02-19 | v1.2 | 24 項 Todo 全部完成並完成前端整合 |

---

*文件路徑：`docs/agent-field-ui-plan.md`*
